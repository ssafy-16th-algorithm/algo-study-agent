export const notionSolutionUrls: Partial<Record<string,string>> = {
  "bridge:minkyung": "https://app.notion.com/p/8a1a717ec99e820a8e3e0107c8d3437b",
  "bridge:taekgi": "https://app.notion.com/p/9c9a717ec99e8381b67a0117c2932117",
  "bridge:jonghyuck": "https://app.notion.com/p/a77a717ec99e8235b314018f3f6e42ee",
  "bridge:yeajeong": "https://app.notion.com/p/a96a717ec99e82fc8a668156d641a6ab",
  "processor:minkyung": "https://app.notion.com/p/148a717ec99e82f3be2601065d9b8a16",
  "processor:jonghyuck": "https://app.notion.com/p/58da717ec99e827791b30167aab63128",
  "processor:taekgi": "https://app.notion.com/p/9b9a717ec99e82ce8e75812fbb3ebd26",
  "processor:yeajeong": "https://app.notion.com/p/faaa717ec99e82ca804a81d4d30de44f",
  "prerequisite:taekgi": "https://app.notion.com/p/1cca717ec99e8299bdbf01c5489be126",
  "prerequisite:yeajeong": "https://app.notion.com/p/62ea717ec99e83e09fdc0135a46caaa7",
  "prerequisite:jonghyuck": "https://app.notion.com/p/7a7a717ec99e8263bc398111341f099a",
  "prerequisite:minkyung": "https://app.notion.com/p/ba8a717ec99e834ab90101ae931fa413",
  "island:minkyung": "https://app.notion.com/p/22ba717ec99e825a80ce01b7be6d23d2",
  "island:taekgi": "https://app.notion.com/p/527a717ec99e837f889301a8aab52fda",
  "island:yeajeong": "https://app.notion.com/p/74aa717ec99e821a863301c2eb187bc3",
  "island:jonghyuck": "https://app.notion.com/p/8f2a717ec99e8314a0bb8180a791b38c",
  "redundant:minkyung": "https://app.notion.com/p/c68a717ec99e8351b9fe013bb2114d8d",
  "redundant:jonghyuck": "https://app.notion.com/p/cd4a717ec99e83b9b7a201afa4ae91f5",
  "redundant:yeajeong": "https://app.notion.com/p/e03a717ec99e8369a6f1817e300da1fe",
  "redundant:taekgi": "https://app.notion.com/p/3d8a717ec99e82d3b81e81223b5f2f3c",
  "pirate:minkyung": "https://app.notion.com/p/0b4a717ec99e83e3aa5b0115ed3e2b8e",
  "pirate:taekgi": "https://app.notion.com/p/6e4a717ec99e83b38dac01147ad410a7",
  "pirate:yeajeong": "https://app.notion.com/p/8dca717ec99e8265b40c011dc0d9b97d",
  "pirate:jonghyuck": "https://app.notion.com/p/bd9a717ec99e8333a59b01ce53d45a37",
  "ricochet:taekgi": "https://app.notion.com/p/0ada717ec99e8210bdf901c48a8b1f8d",
  "ricochet:minkyung": "https://app.notion.com/p/156a717ec99e83079c3381766c8ad45f",
  "ricochet:jonghyuck": "https://app.notion.com/p/7c8a717ec99e835699030149591babe1",
  "ricochet:yeajeong": "https://app.notion.com/p/997a717ec99e83699ebc01c2a78b8ddc",
  "tree:yeajeong": "https://app.notion.com/p/32da717ec99e82d0bc49811c47702f9d",
  "tree:taekgi": "https://app.notion.com/p/39ea717ec99e8397ae4801856f7110b1",
  "tree:jonghyuck": "https://app.notion.com/p/5a7a717ec99e822188b6014966cc968c",
  "tree:minkyung": "https://app.notion.com/p/69da717ec99e83bc807001aaa4d43f77",
  "menu:minkyung": "https://app.notion.com/p/0eba717ec99e834d9df7016a27055205",
  "menu:yeajeong": "https://app.notion.com/p/3bba717ec99e830e920a0173bcb3467e",
  "menu:taekgi": "https://app.notion.com/p/a2aa717ec99e82fda64b81d2c8affccf",
  "menu:jonghyuck": "https://app.notion.com/p/c63a717ec99e82adaa1a013676deee24",
  "height:jonghyuck": "https://app.notion.com/p/68ba717ec99e820dab40018f41c9ac9a",
  "height:yeajeong": "https://app.notion.com/p/b34a717ec99e82d6b796814f0d5edc1d",
  "height:taekgi": "https://app.notion.com/p/b37a717ec99e8289a51401d5f9b12cfe",
  "height:minkyung": "https://app.notion.com/p/e79a717ec99e8329a57e01c3b5b6f8a4",
  "supply:jonghyuck": "https://app.notion.com/p/fc4a717ec99e83b18114013e2ad4debd",
  "supply:yeajeong": "https://app.notion.com/p/958a717ec99e83a3bdcb017a475461e6",
  "supply:taekgi": "https://app.notion.com/p/3eaa717ec99e82ce963a815fbf041e11",
  "supply:minkyung": "https://app.notion.com/p/a9ba717ec99e824b9a9c8171bb547867",
  "brick:jonghyuck": "https://app.notion.com/p/d00a717ec99e83649b1001e5cebabd13",
  "brick:yeajeong": "https://app.notion.com/p/f43a717ec99e83c0855301afdaa8d39a",
  "brick:taekgi": "https://app.notion.com/p/3e6a717ec99e824b81c881602068be9c",
  "brick:minkyung": "https://app.notion.com/p/88fa717ec99e8357b36281b49dc52864",
  "block:jonghyuck": "https://app.notion.com/p/5e9a717ec99e83e5ac7c01e7b254da1b",
  "block:yeajeong": "https://app.notion.com/p/b91a717ec99e82ef991c0175f15c90a3",
  "block:taekgi": "https://app.notion.com/p/185a717ec99e82118f2c014cfed7bb2c",
  "block:minkyung": "https://app.notion.com/p/c08a717ec99e838094ec81c23ce416e3"
};

export const notionCodeSnapshots: Partial<Record<string,string>> = {
  "bridge:jonghyuck": "package week1;\nimport java.util.*;\nimport java.io.*;\n\n// 다리만들기 2\n// N x M 행렬\n// 0 바다, 1 땅\n// 모든 섬 연결하는 다리의 최솟값 출력. 모든 섬 연결 불가하면 -1\npublic class BOJ_17472 {\n                    //   위 오 아래 왼\n    static int [] dx = {-1, 0, 1, 0};\n    static int [] dy = {0, 1, 0, -1};\n    static int [][] map;\n    static int N;\n    static int M;\n\n    public static void main(String[] args) throws IOException {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringBuilder sb = new StringBuilder();\n\n        StringTokenizer st = new StringTokenizer(br.readLine());\n\n        N = Integer.parseInt(st.nextToken());\n        M = Integer.parseInt(st.nextToken());\n\n        map = new int[N][M];\n\n        for(int i = 0 ; i < N ; i++){\n            st = new StringTokenizer(br.readLine());\n\n            for(int j = 0 ; j < M ; j++){\n                map[i][j] = Integer.parseInt(st.nextToken());\n            }\n        }\n\n\n        // think1 : 섬의 가장자리에서, 방향을 바꾸지 않고 탐색 -> 다른섬을 만난다면 해당 거리가 다리의 길이다.\n        // 섬을 구분 -> 섬에 번호를 붙인다. (DFS)\n\n        // 재귀를 위한 초기값 2 => 섬은 다른 섬일떄마다 2,3,4.. 넘버링\n        int islandNum = 2;\n        for (int row = 0; row < N; row++){\n            for(int col = 0; col < M; col++){\n                if(map[row][col] == 1){\n                    numberingIsland(row, col, islandNum);\n                    islandNum ++;\n                }\n            }\n        }\n\n        // think 2 : 일단 섬과 섬사이의 최단거리를 구한다? => BFS (제약 : 2이상)\n        // 근데 이제 직선으로 뻗어야하는데..\n        // 섬이 다른걸 구분\n\n\n    }\n    \n\n    // 섬을 구분하기위한 넘버링 DFS\n\n    static void numberingIsland(int row, int col, int islandNum){\n\n        map[row][col] = islandNum;\n\n        for(int i = 0; i < 4 ; i++){\n            int nextRow = row + dx[i];\n            int nextCol = col + dy[i];\n\n\n            // map 밖\n            if(nextRow < 0 || nextRow >= N || nextCol < 0 || nextCol >= M){\n                continue;\n            }\n\n            // 섬 아니라면\n            if(map[nextRow][nextCol] != 1){\n                continue;\n            }\n\n            numberingIsland(nextRow, nextCol, islandNum);\n        }\n\n    }\n\n    // 만들 수 있는 다리를 탐색하는 함수\n    static void makeBridge(int row, int col){\n        int startIsland = map[row][col];\n\n        int bridgeLength = 0;\n        while(true){\n            for(int i = 0; i < 4; i++){\n                int nextRow = row + dx[i];\n                int nextCol = col + dy[i];\n\n                if (nextRow < 0 || nextRow >= N || nextCol < 0 || nextCol >= M) {\n                    return;\n                }\n                // 바다라면\n                if(map[nextRow][nextCol] == 0){\n                    bridgeLength ++;\n\n                }\n\n\n            }\n        }\n\n    }\n\n}",
  "bridge:yeajeong": "package yeajeong;\n\nimport java.util.ArrayDeque;\nimport java.util.ArrayList;\nimport java.util.Arrays;\nimport java.util.List;\nimport java.util.Queue;\nimport java.io.BufferedReader;\nimport java.io.FileInputStream;\nimport java.io.InputStreamReader;\nimport java.util.StringTokenizer;\n\npublic class Main {\n\tstatic int INF = Integer.MAX_VALUE;\n\tstatic int[] dr = { 0, 0, 1, -1 };\n\tstatic int[] dc = { 1, -1, 0, 0 };\n\n\tstatic int N;\n\tstatic int M;\n\tstatic int[][] myMap;\n\tstatic int[][] isIsland;\n\n\tpublic static void main(String[] args) throws Exception {\n\n\t\tif (System.getProperty(\"LOCAL\") != null) {\n\t\t\tSystem.setIn(new FileInputStream(\"input.txt\"));\n\t\t}\n\n\t\tBufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n\n\t\tStringTokenizer st = new StringTokenizer(br.readLine());\n\n\t\t// 선입 선출\n\t\tQueue<int[]> q = new ArrayDeque<>();\n\n\t\tN = Integer.parseInt(st.nextToken());\n\t\tM = Integer.parseInt(st.nextToken());\n\n\t\tmyMap = new int[N][M];\n\t\tisIsland = new int[N][M];\n\n\t\t// 섬배열 입렫받기\n\t\tfor (int i = 0; i < N; i++) {\n\t\t\tst = new StringTokenizer(br.readLine());\n\t\t\tfor (int j = 0; j < M; j++) {\n\t\t\t\tmyMap[i][j] = Integer.parseInt(st.nextToken());\n\n\t\t\t}\n\t\t}\n\n\t\t// 섬개수\n\t\tint count = 0;\n\t\t\n\t\t// 섬 찾아서 번호 붙이기\n\t\tfor (int i = 0; i < N; i++) {\n\t\t\tfor (int j = 0; j < M; j++) {\n\n\t\t\t\t// 섬이고 방문 안했다면 bfs,\n\t\t\t\tif (myMap[i][j] == 1 && (isIsland[i][j] == 0)) {\n\t\t\t\t\tcount++;\n\t\t\t\t\tbfs(i, j, count);\n\t\t\t\t}\n\t\t\t} // end search island\n\t\t}\n\t\t\n\t\t\n\t\tint[][] bridge = new int[count + 1][count + 1];\n\n\t\tfor (int i = 0; i <= count; i++) {\n\t\t\tArrays.fill(bridge[i], INF);\n\t\t}\n\n\t\t\n\t\t// 다리 건설\n\t\tfor (int i = 0; i < N; i++) {\n\t\t\tfor (int j = 0; j < M; j++) {\n\n\t\t\t\tif (isIsland[i][j] > 0) {\n\t\t\t\t\tint cr = i;\n\t\t\t\t\tint cc = j;\n\t\t\t\t\tint myNum = isIsland[i][j];\n\t\t\t\t\t// 섬이 얼마나 연결되어있는지 사방향 탐색\n\t\t\t\t\tfor (int d = 0; d < 4; d++) {\n\n\t\t\t\t\t\tint nc = cc;\n\t\t\t\t\t\tint nr = cr;\n\t\t\t\t\t\tint length = 0;\n\n\t\t\t\t\t\twhile (true) {\n\t\t\t\t\t\t\tnr += dr[d];\n\t\t\t\t\t\t\tnc += dc[d];\n\n\t\t\t\t\t\t\tif (nr < 0 || nr > N - 1 || nc < 0 || nc > M - 1)\n\t\t\t\t\t\t\t\tbreak;\n\n\t\t\t\t\t\t\t// 바다일 경우 -> 방향증가\n\t\t\t\t\t\t\tif (isIsland[nr][nc] == 0) {\n\t\t\t\t\t\t\t\tlength++;\n\t\t\t\t\t\t\t\tcontinue;\n\n\t\t\t\t\t\t\t}\n\n\t\t\t\t\t\t\t// 같은 섬일 경우 -> 종료\n\t\t\t\t\t\t\tif (isIsland[nr][nc] > 0 && isIsland[nr][nc] == myNum)\n\t\t\t\t\t\t\t\tbreak;\n\n\t\t\t\t\t\t\t// 다른 섬일경우 -> 길이 체크 -> 저장\n\t\t\t\t\t\t\tif (isIsland[nr][nc] > 0 && isIsland[nr][nc] != myNum) {\n\t\t\t\t\t\t\t\tif (length >= 2) {\n\t\t\t\t\t\t\t\t\tint nextNum = isIsland[nr][nc];\n\t\t\t\t\t\t\t\t\tbridge[myNum][nextNum] = Math.min(bridge[myNum][nextNum], length);\n\t\t\t\t\t\t\t\t\tbridge[nextNum][myNum] = Math.min(bridge[nextNum][myNum], length);\n\n\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t\tbreak;\n\t\t\t\t\t\t\t}\n\n\t\t\t\t\t\t}\n\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t} // end building bridge\n\n\t\tList<Edge> edges = new ArrayList<>();\n\n\t\tfor (int i = 1; i <= count; i++) {\n\t\t\tfor (int j = i + 1; j <= count; j++) {\n\t\t\t\tif (bridge[i][j] != INF)\n\t\t\t\t\tedges.add(new Edge(i, j, bridge[i][j]));\n\t\t\t}\n\t\t} // end create edges ;\n\n\t\tedges.sort((a, b) -> a.length - b.length);\n\n\t\tint[] parent = new int[count + 1];\n\t\tfor (int i = 1; i <= count; i++) {\n\t\t\tparent[i] = i;\n\t\t}\n\n\t\tint total = 0;\n\t\tint selected = 0;\n\n\t\tfor (Edge edge : edges) {\n\n\t\t\tif (union(parent, edge.from, edge.to)) {\n\t\t\t\ttotal += edge.length;\n\t\t\t\tselected++;\n\t\t\t}\n\n\t\t\t// count-1 은 다리개수\n\t\t\tif (selected == count - 1)\n\t\t\t\tbreak;\n\t\t}\n\t\t\n\t\tSystem.out.println(selected == count-1 ? total : -1);\n\n\t} // end\n\n\n\tstatic void bfs(int sr, int sc, int islandNo) {\n\t\tQueue<int[]> q = new ArrayDeque<>();\n\n\t\tisIsland[sr][sc] = islandNo;\n\t\tq.offer(new int[] { sr, sc });\n\n\t\twhile (!(q.isEmpty())) {\n\n\t\t\tint[] current = q.poll();\n\t\t\tint cr = current[0];\n\t\t\tint cc = current[1];\n\n\t\t\t// 섬이 얼마나 연결되어있는지 사방향 탐색\n\t\t\tfor (int d = 0; d < 4; d++) {\n\n\t\t\t\tint nc = cc + dc[d];\n\t\t\t\tint nr = cr + dr[d];\n\n\t\t\t\t// 맵 범위체크\n\t\t\t\tif (nr < 0 || nr > N - 1 || nc < 0 || nc > M - 1)\n\t\t\t\t\tcontinue;\n\t\t\t\tif (isIsland[nr][nc] != 0)\n\t\t\t\t\tcontinue;\n\t\t\t\tif (myMap[nr][nc] != 1)\n\t\t\t\t\tcontinue;\n\n\t\t\t\tisIsland[nr][nc] = islandNo;\n\t\t\t\tq.offer(new int[] { nr, nc });\n\n\n\t\t\t}\n\n\t\t}\n\n\t}\n\n\tstatic class Edge {\n\t\tint from;\n\t\tint to;\n\t\tint length;\n\n\t\tEdge(int from, int to, int length) {\n\t\t\tthis.from = from;\n\t\t\tthis.to = to;\n\t\t\tthis.length = length;\n\n\t\t}\n\n\t}\n\n\tstatic int find(int[] parent, int x) {\n\t\t// 자기자신이면 x반환\n\t\tif (parent[x] == x)\n\t\t\treturn x;\n\n\t\t// 자기 자신이 아니면\n\t\treturn parent[x] = find(parent, parent[x]);\n\t}\n\n\tstatic boolean union(int[] parent, int a, int b) {\n\t\tint rootA = find(parent, a);\n\t\tint rootB = find(parent, b);\n\n\t\tif (rootA == rootB) {\n\t\t\treturn false;\n\t\t}\n\n\t\tparent[rootB] = rootA;\n\t\treturn true;\n\t}\n}",
  "processor:jonghyuck": "/// //////////////////////////////////////////////////////////////////////////////////////////\n// 기본 제공코드는 임의 수정해도 관계 없습니다. 단, 입출력 포맷 주의\n// 아래 표준 입출력 예제 필요시 참고하세요.\n// 표준 입력 예제\n// int a;\n// double b;\n// char g;\n// String var;\n// long AB;\n// a = sc.nextInt();                           // int 변수 1개 입력받는 예제\n// b = sc.nextDouble();                        // double 변수 1개 입력받는 예제\n// g = sc.nextByte();                          // char 변수 1개 입력받는 예제\n// var = sc.next();                            // 문자열 1개 입력받는 예제\n// AB = sc.nextLong();                         // long 변수 1개 입력받는 예제\n/// //////////////////////////////////////////////////////////////////////////////////////////\n// 표준 출력 예제\n// int a = 0;                            \n// double b = 1.0;               \n// char g = 'b';\n// String var = \"ABCDEFG\";\n// long AB = 12345678901234567L;\n//System.out.println(a);                       // int 변수 1개 출력하는 예제\n//System.out.println(b); \t\t       \t\t\t\t\t\t // double 변수 1개 출력하는 예제\n//System.out.println(g);\t\t       \t\t\t\t\t\t // char 변수 1개 출력하는 예제\n//System.out.println(var);\t\t       \t\t\t\t   // 문자열 1개 출력하는 예제\n//System.out.println(AB);\t\t       \t\t\t\t     // long 변수 1개 출력하는 예제\n/// //////////////////////////////////////////////////////////////////////////////////////////\npackage week1;\n\nimport java.util.*;\nimport java.io.*;\n\n/*\n   사용하는 클래스명이 Solution 이어야 하므로, 가급적 Solution.java 를 사용할 것을 권장합니다.\n   이러한 상황에서도 동일하게 java Solution 명령으로 프로그램을 수행해볼 수 있습니다.\n */\nclass SWEA_프로세서연결하기 {\n    static int N;\n    static int[][] map;\n    static int[] dx = {-1, 0, 1, 0};\n    static int[] dy = {0, 1, 0, -1};\n    static boolean[][] visited;\n    static ArrayList<int[]> cores;\n    static int maxConnect;\n    static int minLength;\n\n\n    public static void main(String args[]) throws Exception {\n\t\t/*\n\t\t   아래의 메소드 호출은 앞으로 표준 입력(키보드) 대신 input.txt 파일로부터 읽어오겠다는 의미의 코드입니다.\n\t\t   여러분이 작성한 코드를 테스트 할 때, 편의를 위해서 input.txt에 입력을 저장한 후,\n\t\t   이 코드를 프로그램의 처음 부분에 추가하면 이후 입력을 수행할 때 표준 입력 대신 파일로부터 입력을 받아올 수 있습니다.\n\t\t   따라서 테스트를 수행할 때에는 아래 주석을 지우고 이 메소드를 사용하셔도 좋습니다.\n\t\t   단, 채점을 위해 코드를 제출하실 때에는 반드시 이 메소드를 지우거나 주석 처리 하셔야 합니다.\n\t\t */\n        //System.setIn(new FileInputStream(\"res/input.txt\"));\n\n\t\t/*\n\t\t   표준입력 System.in 으로부터 스캐너를 만들어 데이터를 읽어옵니다.\n\t\t */\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n\n        int T = Integer.parseInt(br.readLine());\n\n        StringBuilder sb = new StringBuilder();\n\n\t\t/*\n\t\t   여러 개의 테스트 케이스가 주어지므로, 각각을 처리합니다.\n\t\t*/\n        // Point : (1순위)<최대한 많은 Core에 전원을 연결한 상태>의 (2순위)<전선의 최소값>\n        for (int test_case = 1; test_case <= T; test_case++) {\n            N = Integer.parseInt(br.readLine());\n            map = new int[N][N];\n\n            // false = 비어 있는 칸\n            // true = 코어 또는 설치된 전선이 있는 칸\n            visited = new boolean[N][N];\n\n            // 코어의 좌표를 저장할 list\n            cores = new ArrayList<>();\n            // 최대 연결 수\n            maxConnect = 0;\n            // 최소 길이\n            minLength = Integer.MAX_VALUE;\n\n            for (int row = 0; row < N; row++) {\n                StringTokenizer st = new StringTokenizer(br.readLine());\n\n                for (int col = 0; col < N; col++) {\n                    map[row][col] = Integer.parseInt(st.nextToken());\n                    // 가장자리 제외한 core 좌표 저장\n                    if (map[row][col] == 1) {\n                        // 코어가 있으면 지나갈 수 없다.\n                        visited[row][col] = true;\n                        if (0 < row && row < N - 1 && 0 < col && col < N - 1) {\n                            cores.add(new int[]{row, col});\n                        }\n                    }\n                }\n            }\n            dfs(0, 0, 0);\n            sb.append(\"#\").append(test_case).append(\" \").append(minLength).append(\"\\n\");\n        }\n        System.out.print(sb);\n    }\n\n\n    // 코어의 연결방향 4방향 탐색 + 전선의 길이 최소가 되도록 => dfs\n    // 탐색하고 전선을 제거해야 방해되지 않음 (원상 복구) => 백트래킹\n    static void dfs(int count, int length, int connect) {\n\n        // 모든 내부 코어를 처리한 경우\n        if (count == cores.size()) {\n            // (1순위)\n            // 기존보다 더 많은 코어를 연결했다면\n            if (connect > maxConnect) {\n                maxConnect = connect;\n                minLength = length;\n            }\n            // (2순위)\n            // 연결된 코어 수가 같다면\n            // 전선 길이가 더 짧은 값을 선택\n            else if (connect == maxConnect) {\n                minLength = Math.min(minLength, length);\n            }\n\n            return;\n        }\n\n        // 현재 코어의 좌표\n        int[] currentCore = cores.get(count);\n        int row = currentCore[0];\n        int col = currentCore[1];\n\n        // 4방향 연결\n        for (int direction = 0; direction < 4; direction++) {\n\n            int currentLength = connectWire(row, col, direction);\n\n            // 해당 방향으로 연결할 수 없는 경우\n            if (currentLength == 0) {\n                continue;\n            }\n\n            // 현재 방향으로 전선이 설치된 상태에서 다음 코어 탐색(현재 코어를 연결한 경우)\n            dfs(count + 1, length + currentLength, connect + 1);\n\n            // 다른 방향을 탐색하기 위해 현재 설치한 전선을 제거\n            removeWire(row, col, direction);\n        }\n        // 미연결 경우는 마지막에 한 번만 탐색\n        dfs(count + 1, length, connect);\n    }\n\n    // 해당 방향으로 전선을 설치 / 성공하면 설치한 전선 길이를 반환하고, 중간에 코어나 기존 전선을 만나면 0을 반환\n    static int connectWire(int row, int col, int direction) {\n        int length = 0;\n        int nextRow = row + dx[direction];\n        int nextCol = col + dy[direction];\n\n        // 우선 실제 visited 배열에 표시하면서 이동. 만약 중간에 막히면 지금까지 표시한 전선을 다시 false로 복구\n        Queue<int[]> wirePositions = new ArrayDeque<>();\n        while (nextRow >= 0 && nextRow < N && nextCol >= 0 && nextCol < N) {\n            // 코어나 기존 전선을 만난 경우\n            if (visited[nextRow][nextCol]) {\n                // 임시로 설치한 전선 제거\n                while (!wirePositions.isEmpty()) {\n                    int[] position = wirePositions.poll();\n                    int wireRow = position[0];\n                    int wireCol = position[1];\n\n                    visited[wireRow][wireCol] = false;\n                }\n                return 0;\n            }\n\n            // 현재 칸에 전선 설치\n            visited[nextRow][nextCol] = true;\n            wirePositions.offer(new int[]{nextRow, nextCol});\n            length++;\n\n            nextRow += dx[direction];\n            nextCol += dy[direction];\n        }\n\n        return length;\n    }\n\n    // dfs 끝나고 전선 제거\n    static void removeWire(int row, int col, int direction) {\n        int nextRow = row + dx[direction];\n        int nextCol = col + dy[direction];\n\n        while (nextRow >= 0 && nextRow < N && nextCol >= 0 && nextCol < N) {\n            visited[nextRow][nextCol] = false;\n            nextRow += dx[direction];\n            nextCol += dy[direction];\n        }\n    }\n}",
  "processor:taekgi": "package swea;\n\nimport java.util.*;\nimport java.io.*;\n\n// [SW Test 샘플문제] 프로세서 연결하기\npublic class swea_1167 {\n\tstatic int N, max, min;\n\tstatic int processor[][];\n\tstatic int[] dr = { -1, 1, 0, 0 };\n\tstatic int[] dc = { 0, 0, -1, 1 };\n\n\tstatic ArrayList<Node> cores; // 가장자리를 제외한 코어\n\n\tstatic BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n\tstatic StringBuilder sb = new StringBuilder();\n\n\tpublic static void main(String[] args) throws NumberFormatException, IOException {\n\t    int T = Integer.parseInt(br.readLine());\n\n\t    for (int tc = 1; tc <= T; tc++) {\n\t        max = 0;\n\t        min = Integer.MAX_VALUE; // 최소 길이\n\t        cores = new ArrayList<>(); // 가장자리 제외 코어들\n\n\t        N = Integer.parseInt(br.readLine());\n\n\t        processor = new int[N][N];\n\n\t        for (int r = 0; r < N; r++) {\n\t            StringTokenizer st = new StringTokenizer(br.readLine());\n\n\t            for (int c = 0; c < N; c++) {\n\t                processor[r][c] = Integer.parseInt(st.nextToken());\n\n\t                if (processor[r][c] == 1) {\n\t                    if (r != 0 && r != N - 1 && c != 0 && c != N - 1) {\n\t                        cores.add(new Node(r, c));\n\t                    }\n\t                }\n\t            }\n\t        }\n\n\t        dfs(0, 0);\n\n\t        sb.append(\"#\")\n\t          .append(tc)\n\t          .append(\" \")\n\t          .append(min)\n\t          .append(\"\\n\"); // 테스트 케이스별 줄바꿈\n\t    }\n\n\t    System.out.print(sb);\n\t}\n\n\tprivate static void dfs(int index, int coreCnt) {\n\t\tif (index == cores.size()) {\n\t\t\tint line = countLine(); // 전선 길이\n\t\t\tif (max < coreCnt) {\n\t\t\t\tmax = coreCnt;\n\t\t\t\tmin = line;\n\t\t\t} else if (max == coreCnt) { // 코어 개수가 같으면 전선 최소 길이로 결정\n\t\t\t\tif (min > line)\n\t\t\t\t\tmin = line;\n\t\t\t}\n\n\t\t\treturn;\n\t\t}\n\n\t\t// 현재 코어의 행과 열 좌표 가져오기\n\t\tNode cur = cores.get(index);\n\t\tint x = cur.x;\n\t\tint y = cur.y;\n\n\t\tfor (int d = 0; d < 4; d++) {\n\n\t\t\tif (isAvailable(x, y, dr[d], dc[d])) {\n\t\t\t\t// 전선 놓기\n\t\t\t\tsetLine(x, y, dr[d], dc[d], 2);\n\n\t\t\t\t// 다음 코어로 이동\n\t\t\t\tdfs(index + 1, coreCnt + 1);\n\n\t\t\t\t// 놓았던 전선 되돌려 놓기\n\t\t\t\tsetLine(x, y, dr[d], dc[d], 0);\n\t\t\t}\n\n\t\t}\n\n\t\t// 코어 비선택\n\t\tdfs(index + 1, coreCnt);\n\t}\n\n\t// 전선 길이 구하는 함수\n\tprivate static int countLine() {\n\t\tint cnt = 0;\n\n\t\tfor (int r = 0; r < N; r++) {\n\t\t\tfor (int c = 0; c < N; c++) {\n\t\t\t\tif (processor[r][c] == 2)\n\t\t\t\t\tcnt++;\n\t\t\t}\n\t\t}\n\n\t\treturn cnt;\n\t}\n\n\t// 전선 놓는 함수\n\tprivate static void setLine(int x, int y, int dr, int dc, int s) {\n\t\tint copy_x = x, copy_y = y;\n\n\t\twhile (true) {\n\t\t\tcopy_x += dr;\n\t\t\tcopy_y += dc;\n\n\t\t\tif (copy_x < 0 || copy_x > N - 1 || copy_y < 0 || copy_y > N - 1)\n\t\t\t\tbreak;\n\n\t\t\tprocessor[copy_x][copy_y] = s;\n\t\t}\n\t}\n\n\t// 전선을 놓을 수 있는지 여부 확인 함수\n\tprivate static boolean isAvailable(int x, int y, int dr, int dc) {\n\t\tint copy_x = x, copy_y = y;\n\n\t\twhile (true) {\n\t\t\tcopy_x += dr;\n\t\t\tcopy_y += dc;\n\n\t\t\tif (copy_x < 0 || copy_x > N - 1 || copy_y < 0 || copy_y > N - 1)\n\t\t\t\tbreak;\n\n\t\t\t// 중간에 장애물이 있다면 false 반환\n\t\t\tif (processor[copy_x][copy_y] != 0)\n\t\t\t\treturn false;\n\t\t}\n\n\t\treturn true;\n\t}\n\n\tprivate static class Node {\n\t\tint x;\n\t\tint y;\n\n\t\tNode(int x, int y) {\n\t\t\tthis.x = x;\n\t\t\tthis.y = y;\n\t\t}\n\t}\n}",
  "processor:yeajeong": "package codingtest;\n\nimport java.io.BufferedReader;\nimport java.io.IOException;\nimport java.io.InputStreamReader;\nimport java.util.ArrayDeque;\nimport java.util.Queue;\nimport java.util.StringTokenizer;\n\npublic class Main {\n\n\tstatic final int[] dr = { 0, 0, -1, 1, };\n\tstatic final int[] dc = { 1, -1, 0, 0 };\n\tstatic int N;\n\n\tpublic static void main(String[] args) throws IOException {\n\n\t\t// 전략\n\t\t// 프로세서 연결하기\n\t\t// 이전 문제인 다리만들기랑 비슷한문제 근데 좀 더 쉬운느낌\n\t\t// 최대 12정도는 초과 안 날듯\n\t\t// 그래프 및 유니온파인드 필요할수도 ? 아직은 잘 모르겠음\n\n\t\t// bfs로 섬 체크\n\t\t// 방향탐색 -> 사뱡항 탐색 -> 한방향 탐색으로 가장자리 닿는 지 검사.\n\t\t// 최소 거리찾기위해 배열로 최소거리 갱신\n\n\t\tBufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n\t\tint test_case = Integer.parseInt(br.readLine());\n\n\t\tStringTokenizer st = new StringTokenizer(br.readLine());\n\n\t\t// 입력 받기\n\t\tfor (int tc = 0; tc < test_case; tc++) {\n\t\t\tN = Integer.parseInt(st.nextToken());\n\n\t\t\tint[][] myMap = new int[N][N];\n\t\t\tint[][] visited = new int[N][N];\n\t\t\tint[] lines = new int[N + 1];\n\t\t\tQueue<int[]> q = new ArrayDeque<int[]>();\n\t\t\tint count = 0;\n\t\t\tint total = 0;\n\n\t\t\tfor (int i = 0; i < N; i++) {\n\t\t\t\tst = new StringTokenizer(br.readLine());\n\t\t\t\tfor (int j = 0; i < N; j++) {\n\t\t\t\t\tmyMap[i][j] = Integer.parseInt(st.nextToken());\n\t\t\t\t\tif (myMap[i][j] == 1) {\n\t\t\t\t\t\tcount++;\n\t\t\t\t\t\tq.offer(new int[] { i, j, count });\n\t\t\t\t\t}\n\t\t\t\t}\n\n\t\t\t} // end input\n\n\t\t\t// bfs 할 필요 x -> 그냥 완탐 가능\n\t\t\twhile (!q.isEmpty()) {\n\t\t\t\t// 프로세서일때\n\t\t\t\tint current[] = q.poll();\n\t\t\t\tint cr = current[0];\n\t\t\t\tint cc = current[1];\n\t\t\t\tint lineNum = current[2];\n\n\t\t\t\t// 자기자신이 가장자리 일 경우\n\t\t\t\tif (cc == 0 || cc == N - 1 || cr == 0 || cr == N - 1) {\n\t\t\t\t\tcount -= 1;\n\t\t\t\t\tvisited[cr][cc] = 1;\n\t\t\t\t\tcontinue;\n\n\t\t\t\t}\n\n\t\t\t\t// 가장자리 아닌 경우 선을 이읍시다.\n\t\t\t\t// 4방향 계속탐색\n\t\t\t\tfor (int d = 0; d < 4; d++) {\n\t\t\t\t\tint nr;\n\t\t\t\t\tint nc;\n\t\t\t\t\t// 범위 체크\n\t\t\t\t\tint length = 0;\n\t\t\t\t\twhile (true) {\n\t\t\t\t\t\tnr = cc + dr[d];\n\t\t\t\t\t\tnc = cc + dc[d];\n\n\t\t\t\t\t\tif (nr < 0 || nr >= N || nc < 0 || nc >= N) {\n\t\t\t\t\t\t\t// 가장자리 도착시 반복 종료\n\t\t\t\t\t\t\tlines[lineNum] = Math.min(lineNum, length);\n\t\t\t\t\t\t\tbreak;\n\t\t\t\t\t\t}\n\n\t\t\t\t\t\t// 길이면 한방향으로 쭉 진행\n\t\t\t\t\t\tif (myMap[nr][nc] == 0) {\n\t\t\t\t\t\t\tlength++;\n\t\t\t\t\t\t\tcontinue;\n\t\t\t\t\t\t}\n\n\t\t\t\t\t\t// 다른 코어나 전선이면\n\t\t\t\t\t\tif (myMap[nr][nc] == 1) {\n\t\t\t\t\t\t\tlength = 0;\n\t\t\t\t\t\t\tbreak;\n\t\t\t\t\t\t}\n\n\t\t\t\t\t}\n\n\t\t\t\t}\n\n\t\t\t\t// 아닌경우\n\t\t\t}\n\t\t} // end tc\n\t}\n\n}",
  "prerequisite:taekgi": "package boj;\n\nimport java.io.*;\nimport java.util.*;\n\n// 선수과목\npublic class boj_14567 {\n\n\tpublic static void main(String[] args) throws IOException {\n\t\tint n, m; // 전체 과목, 선수 과목\n\t\tint[] semester; // 과목 당 수강하는 학기\n\t\tint[] child; // 선수 과목 개수\n\t\tList<List<Integer>> graph = new ArrayList<>();\n\t\tQueue<Integer> q = new LinkedList<>();\n\n\t\tBufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n\t\tStringBuilder sb = new StringBuilder();\n\n\t\tStringTokenizer st = new StringTokenizer(br.readLine());\n\t\t\n\t\t// 전체 과목과 선수 과목 입력\n\t\tn = Integer.parseInt(st.nextToken());\n\t\tm = Integer.parseInt(st.nextToken());\n\t\t\n\t\t// 크기 지정\n\t\tsemester = new int[n+1];\n\t\tchild = new int[n+1];\n\t\t\n\t\tfor(int i = 0; i <= n; i++) {\n\t\t\tgraph.add(new ArrayList<>());\n\t\t}\n\t\t\n\t\tfor(int i = 0; i < m; i++) {\n\t\t\t// A과목과 B과목 입력\n\t\t\tst = new StringTokenizer(br.readLine());\n\t\t\tint a = Integer.parseInt(st.nextToken());\n\t\t\tint b = Integer.parseInt(st.nextToken());\n\t\t\t\n\t\t\tgraph.get(a).add(b); // a 과목에서 뻗어나가는 b 과목 추가\n\t\t\tchild[b]++; // b 과목의 선수과목 개수 증가\n\t\t}\n\t\t\n\t\tfor(int i = 1; i <= n; i++) {\n\t\t\t// 선수 과목이 하나도 없는 과목을 q에 추가\n\t\t\tif(child[i] == 0) {\n\t\t\t\tq.offer(i);\n\t\t\t\tsemester[i] = 1; // 1학기 수강\n\t\t\t}\n\t\t}\n\t\t\n\t\twhile(!q.isEmpty()) {\n\t\t\tint cur = q.poll();\n\t\t\t\n\t\t\t// 해당 과목의 선수과목을 가져와 \n\t\t\tfor(int g : graph.get(cur)) {\n\t\t\t\tchild[g]--; // 연결된 과목 개수 감소\n\t\t\t\tsemester[g] = Math.max(semester[g], semester[cur]+1); // 다음 과목의 수강 학기를 현재 과목의 수강 다음 학기로 변경\n\t\t\t\t\n\t\t\t\tif(child[g] == 0)\n\t\t\t\t\tq.offer(g);\n\t\t\t}\n\t\t}\n\t\t\n\t\tfor(int i = 1; i <= n; i++) {\n\t\t\tsb.append(semester[i]).append(\" \");\n\t\t}\n\t\t\n\t\tSystem.out.println(sb);\n\n\t}\n\n}",
  "prerequisite:jonghyuck": "package week1;\nimport java.util.*;\nimport java.io.*;\n\n// 선수과목\n// 위상정렬\npublic class BOJ_14567 {\n    static int N;\n    static int M;\n\n    // graph[i] : i를 선수과목으로 요구하는 과목들 저장\n    static ArrayList<Integer>[] graph;\n    // 각 과목에 필요한 선수과목 개수\n    static int[] indegree;\n    // 학기\n    static int[] semester;\n    \n    public static void main(String[] args) throws IOException {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n\n        StringBuilder sb = new StringBuilder();\n\n        StringTokenizer st = new StringTokenizer(br.readLine());\n\n        N = Integer.parseInt(st.nextToken());\n        M = Integer.parseInt(st.nextToken());\n\n        graph = new ArrayList[N+1];\n        indegree = new int[N+1];\n        semester = new int[N+1];\n\n        // subject별 그래프 객체 생성\n        for (int subject = 1; subject <= N; subject++){\n            graph[subject] = new ArrayList<>();\n        }\n\n        // graph, indegree에 input 넣기\n        for(int i = 0; i < M ; i++){\n            st = new StringTokenizer(br.readLine());\n            int before = Integer.parseInt(st.nextToken());\n            int after = Integer.parseInt(st.nextToken());\n\n            graph[before].add(after);\n\n            indegree[after]++;\n\n        }\n        /*\n        graph[subject] 형태\n         1 : [2,3]\n         2 : [5]\n         3 : []\n         4 : [5]\n         5 : []\n\n        indegree[after] 형태(진입 차수)\n        indegree[1] = 0\n        indegree[2] = 1\n        indegree[3] = 1\n        indegree[4] = 0\n        indegree[5] = 2\n        indegree[6] = 0\n         */\n\n        sort();\n\n        for(int subject = 1; subject <= N; subject++){\n            sb.append(semester[subject]).append(' ');\n        }\n\n        System.out.println(sb);\n    }\n    // 위상정렬 함수\n    static void sort(){\n        Queue<Integer> queue = new ArrayDeque<>();\n\n        // 선수과목 없는(indegree) 과목 큐에 삽입\n        for(int subject = 1; subject <= N; subject++){\n            if(indegree[subject] == 0){\n                queue.offer(subject);\n                semester[subject] = 1;\n            }\n        }\n        while(!queue.isEmpty()){\n            int current = queue.poll();\n\n            for(int next : graph[current]){\n                indegree[next] --;\n\n                // 더 늦은 선수과목을 기준으로\n                semester[next] = Math.max(semester[next],semester[current] + 1);\n\n                if(indegree[next] == 0){\n                    queue.offer(next);\n                }\n\n            }\n        }\n\n\n    }\n}",
  "island:taekgi": "// 프로그래머스 섬 연결하기\nimport java.util.*;\n\npublic class pgs_섬연결하기 {\n    int[] parent;\n    \n    public int solution(int n, int[][] costs) {\n        int answer = 0;\n    \n        Arrays.sort(costs, Comparator.comparingInt((int[] c) -> c[2])); // costs[i][2]를 기준으로 오름차순 정렬\n        \n        // 각 노드의 부모 노드\n        parent = new int[n];\n        \n        // 처음에는 자기 자신을 초기화\n        for(int i = 0; i < n; i++) {\n            parent[i] = i;\n        }\n        \n        int selectedEdgeCnt = 0; // 선택된 간선의 개수\n        \n        for(int [] c : costs) {\n            int a = c[0]; // 노드\n            int b = c[1]; // 노드\n            int cost = c[2]; // 비용\n            \n            int aParent = findParent(a);\n            int bParent = findParent(b);\n            \n            if(aParent == bParent) continue; // 부모가 같다면 간선을 잇지 않음(사이클 형성 방지)\n            \n            answer += cost; // 두 노드끼리 연결\n            parent[bParent] = aParent; // 두 노드의 부모를 동일하게 설정\n            \n            // 최소비용을 이루는 간선을 순회한 이후에도\n            // costs 배열을 끝까지 순회하는 비효율 방지\n            selectedEdgeCnt++;\n            if(selectedEdgeCnt == n - 1) break;\n        }\n        \n        return answer;\n    }\n    \n    // 각 노드들에 대한 부모 찾기\n    private int findParent(int node) {\n        if(parent[node] == node) return node; // 자기 자신이 부모(0)\n        \n        return parent[node] = findParent(parent[node]); // 재귀호출을 통해 노드를 계속 올라감\n    }\n}",
  "island:yeajeong": "import java.util.*;\n\nclass Solution {\n    public static int[] tree;\n    public int solution(int n, int[][] costs) {\n        \n        Arrays.sort(costs,(a,b)-> Integer.compare(a[2],b[2]));\n        int answer = 0;\n        tree = new int[n+1];\n        \n        for(int i = 0 ; i < n+1 ; i++){\n            tree[i] = -1;\n        }\n\n        int edgeCount = 0;\n        //각각의 섬을 받으면서 연결\n        for(int i = 0 ; i < costs.length ; i++){\n            int[] current = costs[i];\n            \n            int a = current[0];\n            int b = current[1];\n            int cost = current[2];\n            \n            if(uni(a,b)){\n                answer+=cost;\n                edgeCount += 1;\n            }\n            \n            if( edgeCount == n-1){\n                break;\n            }\n            \n            \n        }\n        \n        return answer;\n    }\n    \n    public static void dfs(boolean[] visited,int a,int b,int cost){\n        \n        return ; \n    }\n    public static boolean uni(int u, int v){\n        int pu = find(u);\n        int pv = find(v);\n            \n        //같은경우 그룹 합침\n        if(pu== pv)\n            return false;\n        //v의 루트를 u의 루트로 합침\n        tree[pv] = pu;\n        return true;\n        \n        \n    }\n    \n    public static int find(int u){\n        //루트노드를 찾는 재귀\n        int parentU = tree[u];\n        if(tree[u]==-1){\n            return u;\n        }\n        return tree[u]=find(parentU);\n    }\n}",
  "island:jonghyuck": "// 최소 신장 트리\n// 크루스칼 알고리즘\n//(1) 주어진 그래프의 모든 간선에 대해서, 간선의 연결비용을 낮은 순으로 오름 차순 정렬한다.\n//(2) 정렬된 간선 순서대로 선택하면서, 간선의 양 끝 정점을 Union 한다. 단, 이때 선택된 두 정점이 같은 집합에 속해있다면 사이클(cycle)이 있다고 판단하고 포함시키지 않는다.\n\n\nimport java.util.*;\nimport java.io.*;\npublic class PGS_섬연결하기 {\n\n    // 엣지\n    static int E;\n    // 부모노드\n    static int [] parent;\n\n    public int solution(int n, int[][] costs) {\n        // n은 정점의 개수(Vertex)\n\n        // 간선의 개수(Edges)\n        E = costs.length;\n\n        parent = new int[n];\n        int answer = 0;\n        int bridgeCount =0;\n\n        // 오름차순 정렬\n        Arrays.sort(costs, (a,b) -> Integer.compare(a[2], b[2]));\n\n        // 부모 노드 집합 생성\n        for(int i = 0 ; i < n ; i++){\n            parent[i] = i;\n        }\n        // 낮은 cost부터 크루스칼 알고리즘\n        for (int[] cost : costs){\n\n            int islandA = cost[0];\n            int islandB = cost[1];\n            int bridgeCost = cost[2];\n\n            if(union(islandA, islandB)){\n                answer += bridgeCost;\n                bridgeCount ++;\n\n            }\n\n            if(bridgeCount == n-1){\n                break;\n            }\n\n\n\n        }\n\n\n        return answer;\n    }\n    // 두 그룹을 합친다.\n    static boolean union(int a, int b){\n        int rootA = find(a);\n        int rootB = find(b);\n\n        // 같은 그룹이라면 사이클 생김. => 추가하지않음\n        if(rootA == rootB){\n            return false;\n        }\n        if (rootA > rootB){\n            parent[rootB] = rootA;\n        } else{\n            parent[rootA] = rootB;\n        }\n\n        return true;\n    }\n    // 부모가 같은지 재귀로 찾는다.\n    static int find(int x){\n        if(parent[x] == x){\n            return x;\n        }\n        else{\n            return find(parent[x]);\n        }\n    }\n}",
  "redundant:minkyung": "import java.util.*;\n\nclass Solution {\n    public int solution(String[] board) {\n        int answer = 0;\n        int[] dy = new int[]{0, 0, 1, -1}; // 1, 2, 3, 4\n        int[] dx = new int[]{1, -1, 0, 0};\n        \n        char[][] map = new char[board.length][board[0].length()];\n        int[] start = {0, 0};\n        int[] destination = {0, 0};\n        \n        for (int i = 0; i < board.length; i++) {\n            String s = board[i];\n            for (int j = 0; j < s.length(); j++) {\n                map[i][j] = s.charAt(j);\n                if (s.charAt(j) == 'R') {\n                    start = new int[]{i, j};\n                }\n                if (s.charAt(j) == 'G') {\n                    destination = new int[]{i, j};\n                }\n            }\n        }\n        \n        Deque<int[]> dq = new ArrayDeque<>();\n        int[][] visited = new int[board.length][board[0].length()];\n        dq.offerLast(start);\n        visited[start[0]][start[1]] = 1;\n        \n        while (!dq.isEmpty()) {\n            int[] cur = dq.pollFirst();\n            \n            if (map[cur[0]][cur[1]] == 'G') return visited[cur[0]][cur[1]] - 1;\n          \n            for (int i = 0; i < 4; i++) {\n                int ny = cur[0] + dy[i];\n                int nx = cur[1] + dx[i];\n                if (ny >= 0 && nx >= 0 && ny < board.length && nx < board[0].length() && map[ny][nx] != 'D') {\n                    while (ny + dy[i] >= 0 && nx + dx[i] >= 0 && ny + dy[i] < board.length \n                    && nx + dx[i] < board[0].length() && map[ny + dy[i]][nx + dx[i]] != 'D') {\n                \n                        ny += dy[i];\n                        nx += dx[i];\n                    }\n                    \n                    if (visited[ny][nx] == 0) {\n                        visited[ny][nx] = visited[cur[0]][cur[1]] + 1;\n                        dq.offerLast(new int[]{ny, nx});   \n                    }\n                }  \n                \n            }\n        }\n        \n        if (visited[destination[0]][destination[1]] == 0) {\n            return -1;\n        }\n        else {\n            return visited[destination[0]][destination[1]] - 1;\n        }\n    }\n}",
  "redundant:jonghyuck": "package week2;\n/*\nUnion-Find를 써야하는 문제 판별 :\n두 그룹을 합쳐야 하는 문제\n1) 사이클 발생 여부 확인하는 문제\n2) 같은 집합인지 확인하는 문제\n3) 간선 하나씩 추가\n4) 모든 노드를 최소 비용으로 연결\n5) 최소 신장 트리\n6) 간선 비용의 합을 최소화\n\n1. 합치기: union(a, b)\n2. 같은 그룹인지 확인: find(a) == find(b)\n1번과 2번을 연결한다.\n2번과 3번을 연결한다.\n1번과 3번이 연결되어 있는지 확인한다.\n*/\nclass Solution {\n    // 각 노드의 부모를 저장하는 배열\n    int[] parent;\n    public int[] findRedundantConnection(int[][] edges) {\n\n        // 노드 번호가 1부터 시작하므로 길이를 edges.length + 1로 설정\n        parent = new int[edges.length + 1];\n\n        // 자기 자신을 부모로 설정\n        for (int i = 1; i < parent.length; i++) {\n            parent[i] = i;\n        }\n\n        // 간선을 입력 순서대로 확인\n        for (int[] edge : edges) {\n\n            int a = edge[0];\n            int b = edge[1];\n\n            // 이미 같은 집합이면 이 간선을 추가할 때 사이클 발생\n            if (find(a) == find(b)) {\n                return edge;\n            }\n\n            // 아직 다른 집합이라면 연결\n            union(a, b);\n        }\n\n        return new int[0];\n    }\n\n    // 해당 노드가 속한 집합의 대표 부모 찾기\n    public int find(int node) {\n\n        // 자기 자신이 부모라면 대표 노드\n        if (parent[node] == node) {\n            return node;\n        }\n\n        // 부모를 따라가면서 대표 부모 찾기\n        parent[node] = find(parent[node]);\n\n        return parent[node];\n    }\n\n    // 두 노드가 속한 집합 합치기\n    public void union(int a, int b) {\n\n        int rootA = find(a);\n        int rootB = find(b);\n\n        // b의 대표 부모를 a의 대표 부모 밑으로 연결\n        parent[rootB] = rootA;\n    }\n}",
  "redundant:yeajeong": "import java.util.Arrays;\n\nclass Solution {\n    public int[] es;\n    public int[] findRedundantConnection(int[][] edges) {\n\n\n        es = new int[edges.length + 1]; //1-n\n        int n = edges.length;\n\n        //-1 초기화\n        Arrays.fill(es, -1);\n\n        // 검사해\n        for (int i = 0; i < n; i++) {\n            int[] edge = edges[i];\n            int a = edge[0];\n            int b = edge[1];\n\n            //false면 \n            if (!union(a, b)) {\n                return edge; //간선이 정확히 하나 추가된 구조라 바로 반환해도됨\n            }\n\n        }\n        return edges[n - 1];\n    }\n\n    public  boolean union(int a, int b) {\n        int pa = find(a);\n        int pb = find(b);\n\n        //false 중복이면 false 반환하기\n        if (pa == pb)\n            return false;\n\n        es[pb] = pa;\n\n        return true;\n\n    }\n\n    //부모찾기\n    public  int find(int x) {\n        if (es[x] == -1) {\n            return x;\n        }\n\n        return es[x] = find(es[x]);\n    }\n}",
  "redundant:taekgi": "// 부모가 같다면 이미 연결됐다는 의미이기 때문에\n// 간선 형성하지 말기 (사이클 형성 방지)\nclass Solution {\n    static int[] parent;\n\n    // 부모 노드 찾기\n    public static int find(int x) {\n        if(parent[x] == x) {\n            return x;\n        } // 자기 자신이 부모\n        else {\n            return parent[x] = find(parent[x]);\n        } // 재귀를 통해 부모노드를 계속해서 찾아감\n    }\n\n    public static boolean union(int x, int y) {\n        x = find(x);\n        y = find(y);\n\n        if(x == y) { // 부모가 같다면\n            return false;\n        }\n        parent[x] = y;\n        \n        return true;\n    }\n\n    public int[] findRedundantConnection(int[][] edges) {\n        int n = edges.length;\n        int[] answer = {0, 0};\n\n        // 처음에는 자기 자신으로 초기화\n        parent = new int[n+1];\n        for(int i = 1; i <= n; i++) {\n            parent[i] = i;\n        }\n\n        // 간선을 생성할 필요가 없는 노드 찾기\n        for(int i = 0; i < n; i++) {\n            int a = edges[i][0];\n            int b = edges[i][1];\n\n            if(!union(a, b)) {\n                answer = new int[] {a, b};\n            }\n        }\n\n        return answer;\n    }\n}",
  "ricochet:taekgi": "/*\n이 문제의 핵심\n- 행과 열을 장애물이나 경계까지 미끄러진 후의 위치를 다음 위치로 갱신하여 최소 이동 횟수를 반환\n\n1. board에서 R의 위치를 찾는다.\n2. R을 큐에 넣고 방문 처리한다.\n3. 큐에서 현재 위치를 꺼낸다.\n4. 상하좌우 각각 끝까지 미끄러진다.\n5. 멈춘 위치가 방문하지 않은 곳이면 큐에 넣는다.\n6. 멈춘 위치가 G라면 이동 횟수를 반환한다.\n7. 큐가 빌 때까지 G에 도달하지 못하면 -1을 반환한다.\n*/\n\nimport java.util.*;\n\nclass Solution {\n    public int solution(String[] board) {\n        int answer = 0;\n        int rows = board.length;\n        int cols = board[0].length();\n        \n        boolean[][] visited = new boolean[rows][cols];\n        Queue<int[]> queue = new LinkedList<>();\n        \n        // 상, 하, 좌, 우\n        int[] dr = {-1, 1, 0, 0};\n        int[] dc = {0, 0, -1, 1};\n        \n        // R 위치 찾기\n        for(int r = 0; r < rows; r++) {\n            for(int c = 0; c < cols; c++) {\n                if(board[r].charAt(c) == 'R') {\n                    // 이 문제의 핵심을 해결하기 위해 행, 열, 이동횟수를 사용\n                    queue.offer(new int[]{r, c, 0}); // {행, 열, 이동 횟수}\n                    visited[r][c] = true;\n                }\n            }\n        }\n        \n        // BFS\n        while(!queue.isEmpty()) {\n            // 현재 위치 꺼내기\n            int[] current = queue.poll();\n            \n            int r = current[0];\n            int c = current[1];\n            int moveCnt = current[2];\n            \n            // 현재 위치가 목표 지점이라면 이동 횟수 반환\n            if(board[r].charAt(c) == 'G') {\n                return moveCnt;\n            }\n            \n            // 상하좌우 끝까지 이동하기\n            for(int dir = 0; dir < 4; dir++) {\n                int nr = r;\n                int nc = c;\n                \n                while(true) {\n                    // 이동\n                    int mr = nr + dr[dir];\n                    int mc = nc + dc[dir];\n                    \n                    // 보드 밖이거나 장애물 지점이면 현재 위치에서 정지\n                    if(mr < 0 || mr >= rows || mc < 0 || mc >= cols\n                      || board[mr].charAt(mc) == 'D') {\n                        break;\n                    }\n                \n                    // 현재 지점을 이동한 위치로 갱신\n                    nr = mr;\n                    nc = mc;\n                }\n                \n                // 도착한 지점이 방문한 지점이 아니라면 큐에 추가\n                if(!visited[nr][nc]) {\n                    visited[nr][nc] = true; // 방문 표시\n                    queue.offer(new int[]{nr, nc, moveCnt + 1});\n                }\n            }\n        }\n        \n        // G에 도달할 수 없을 경우 -1 반환\n        return -1;\n    }\n}",
  "ricochet:jonghyuck": "package week3;\n\nimport java.util.ArrayDeque;\nimport java.util.Queue;\n\nclass PGS_리코챗로봇 {\n    static int dx [] = {-1, 0, 1, 0};\n    static int dy [] = {0, 1, 0 ,-1};\n    public int solution(String[] board) {\n        int startRow = 0;\n        int startCol = 0;\n\n\n        for(int i = 0; i < board.length ; i++){\n            for(int j = 0; j < board[0].length() ; j++){\n                if(board[i].charAt(j) == 'R'){\n                    startRow = i;\n                    startCol = j;\n                }\n\n            }\n\n        }\n        int answer = 0;\n        answer = findG( board,  startRow,  startCol);\n        return answer;\n    }\n    // 벽이나 'D'라면 멈추고, 멈췄을때 해당 칸이 'G'라면 움직임을 return\n    public int findG(String[] board,int startRow, int startCol){\n        int count = 0;\n        int rowSize = board.length;\n        int colSize = board[0].length();\n        boolean [][] visited = new boolean[rowSize][colSize];\n\n        Queue<int[]> queue = new ArrayDeque<>();\n        // {현재Row, 현재Col, 움직인수}\n        queue.offer(new int[] {startRow, startCol, 0});\n        visited[startRow][startCol] = true;\n\n        while(!queue.isEmpty()){\n            int current[] = queue.poll();\n            int currentRow = current[0];\n            int currentCol = current[1];\n            int moveCount = current[2];\n\n            if(board[currentRow].charAt(currentCol) == 'G'){\n                return moveCount;\n            }\n\n            for(int direction = 0; direction < 4; direction++){\n                int nextRow = currentRow;\n                int nextCol = currentCol;\n\t\t\t\t\t\t\t\t// 해당 방향으로 쭉 밀고간다. 장애물 or 밖이면 break\n                while(true){\n                    int moveRow = nextRow + dx[direction];\n                    int moveCol = nextCol + dy[direction];\n\n                    if(moveRow < 0 || moveRow >= rowSize || moveCol <0 || moveCol >= colSize){\n                        break;\n                    }\n                    if(board[moveRow].charAt(moveCol) == 'D'){\n                        break;\n                    }\n                    nextRow = moveRow;\n                    nextCol = moveCol;\n\n                }\n                // 같은 위치에서 못움직일 때\n                if(nextRow == currentRow && nextCol == currentCol){\n                    continue;\n                }\n                //moveCount ++; 을 쓰면 안되고, 반드시 큐에 직접 moveCount+1을 넣어야한다.\n                if(!visited[nextRow][nextCol]){\n                    visited[nextRow][nextCol] = true;\n                    queue.offer(new int[]{nextRow, nextCol, moveCount+1});\n\n                }\n\n            }\n        }\n        return -1;\n\n\n    }\n\n}",
  "ricochet:yeajeong": "import java.util.*;\n\nclass Solution {\n  \n    public int solution(String[] board) {\n        int answer = Integer.MAX_VALUE;\n        int[] dr = {-1,1,0,0};\n        int[] dc = {0,0, -1, 1 };\n        \n        \n        //queue에 최초 시작 좌표넣음\n        Queue<int[]> q = new ArrayDeque<>();\n        int N = board.length;\n        int M = board[0].length();\n        int [][] visited = new int[N][M];\n        String[][] matrix = new String[N][M];\n        \n        \n        for(int i = 0 ; i < N ; i++){\n            String[] array = board[i].split(\"\");\n          for(int j = 0 ; j <M ; j++){\n              matrix[i][j] = array[j];\n              if(matrix[i][j].equals(\"R\")) q.offer(new int[]{i,j,0});\n          }  \n        }\n        \n        //큐뽑고 돌기\n        while(!q.isEmpty()){\n            int[] current = q.poll();\n            int cr = current[0];\n            int cc = current[1];\n            int count = current[2];\n            //나 혹시 정답인지 점검\n            if(matrix[cr][cc].equals(\"G\")) {\n                return count;\n                };\n            \n            //방향돌기\n            for(int i = 0 ; i < 4 ; i++){\n                \n                int nr = cr; \n                int nc = cc;\n                //쭉진행\n                while(nr >= 0 && nr <N && nc>=0 && nc <M && !(matrix[nr][nc].equals(\"D\"))){\n                    nr += dr[i];\n                    nc += dc[i];\n                }//벽을만나거나 범위초과 -> 멈춘다는뜻\n                //한칸뒤로가\n\n                nr -= dr[i];\n                nc -= dc[i];\n                    \n                if(visited[nr][nc] ==1) continue; //멈춘곳이 방문한 적 있으면 기록안함\n                //멈춘곳이 처음 간 곳이면 기록 \n                q.offer(new int[]{nr,nc, count+1});\n                visited[nr][nc] = 1;\n                \n            }\n\n        }\n        \n        return answer == Integer.MAX_VALUE ? -1 : answer ;\n    }\n}",
  "tree:taekgi": "import java.util.Scanner;\nimport java.io.FileInputStream;\n\nclass Solution\n{\n\tpublic static void main(String args[]) throws Exception\n\t{\n\t\tBufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringBuilder sb = new StringBuilder();\n        \n\t\tint T = Integer.parseInt(br.readLine());\n\t\n\t\tfor(int tc = 1; tc <= T; tc++)\n\t\t{\n\t\t\tint N = Integer.parseInt(br.readLine());   \n            \n            int[] trees = new int[N];\n            int maxHeight = 0;\n            \n            StringTokenizer st = new StringTokenizer(br.readLine());\n            for(int i = 0; i < N; i++) {\n                trees[i] = Integer.parseInt(st.nextToken());\n                maxHeight = Math.max(maxHeight, trees[i]); // 최대 높이 갱신\n\t\t\t}\n            \n            int oneCount = 0; // +1이 필요한 횟수\n            int twoCount = 0; // +2이 필요한 횟수\n            \n            // 각 나무별 필요한 횟수 정리\n            int[] diff = new int[N];\n            for(int i = 0; i < N; i++) {\n                diff[i] = maxHeight - trees[i];\n            \n            while(\n\t}\n}",
  "tree:jonghyuck": "/////////////////////////////////////////////////////////////////////////////////////////////\n// 기본 제공코드는 임의 수정해도 관계 없습니다. 단, 입출력 포맷 주의\n// 아래 표준 입출력 예제 필요시 참고하세요.\n// 표준 입력 예제\n// int a;\n// double b;\n// char g;\n// String var;\n// long AB;\n// a = sc.nextInt();                           // int 변수 1개 입력받는 예제\n// b = sc.nextDouble();                        // double 변수 1개 입력받는 예제\n// g = sc.nextByte();                          // char 변수 1개 입력받는 예제\n// var = sc.next();                            // 문자열 1개 입력받는 예제\n// AB = sc.nextLong();                         // long 변수 1개 입력받는 예제\n/////////////////////////////////////////////////////////////////////////////////////////////\n// 표준 출력 예제\n// int a = 0;                            \n// double b = 1.0;               \n// char g = 'b';\n// String var = \"ABCDEFG\";\n// long AB = 12345678901234567L;\n//System.out.println(a);                       // int 변수 1개 출력하는 예제\n//System.out.println(b); \t\t       \t\t\t\t\t\t // double 변수 1개 출력하는 예제\n//System.out.println(g);\t\t       \t\t\t\t\t\t // char 변수 1개 출력하는 예제\n//System.out.println(var);\t\t       \t\t\t\t   // 문자열 1개 출력하는 예제\n//System.out.println(AB);\t\t       \t\t\t\t     // long 변수 1개 출력하는 예제\n/////////////////////////////////////////////////////////////////////////////////////////////\nimport java.io.BufferedReader;\nimport java.io.InputStreamReader;\nimport java.util.Scanner;\nimport java.io.FileInputStream;\nimport java.util.*;\nimport java.io.*;\n/*\n   사용하는 클래스명이 Solution 이어야 하므로, 가급적 Solution.java 를 사용할 것을 권장합니다.\n   이러한 상황에서도 동일하게 java Solution 명령으로 프로그램을 수행해볼 수 있습니다.\n */\nclass SWEA_나무높이\n{\n    public static void main(String args[]) throws Exception\n    {\n\t\t/*\n\t\t   아래의 메소드 호출은 앞으로 표준 입력(키보드) 대신 input.txt 파일로부터 읽어오겠다는 의미의 코드입니다.\n\t\t   여러분이 작성한 코드를 테스트 할 때, 편의를 위해서 input.txt에 입력을 저장한 후,\n\t\t   이 코드를 프로그램의 처음 부분에 추가하면 이후 입력을 수행할 때 표준 입력 대신 파일로부터 입력을 받아올 수 있습니다.\n\t\t   따라서 테스트를 수행할 때에는 아래 주석을 지우고 이 메소드를 사용하셔도 좋습니다.\n\t\t   단, 채점을 위해 코드를 제출하실 때에는 반드시 이 메소드를 지우거나 주석 처리 하셔야 합니다.\n\t\t */\n        //System.setIn(new FileInputStream(\"res/input.txt\"));\n\n\t\t/*\n\t\t   표준입력 System.in 으로부터 스캐너를 만들어 데이터를 읽어옵니다.\n\t\t */\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n\n\n        int T = Integer.parseInt(br.readLine());\n\t\t/*\n\t\t   여러 개의 테스트 케이스가 주어지므로, 각각을 처리합니다.\n\t\t*/\n\n        for(int test_case = 1; test_case <= T; test_case++)\n        {\n            StringBuilder sb = new StringBuilder();\n\n            int N = Integer.parseInt(br.readLine());\n            int [] trees = new int [N];\n            int maxTree = 0;\n            StringTokenizer st = new StringTokenizer(br.readLine());\n\n            for(int i = 0; i < N; i++){\n                trees[i] = Integer.parseInt(st.nextToken());\n                if (maxTree <= trees[i]){\n                    maxTree = trees[i];\n                }\n            }\n            // 하루에 한나무, i % 2 == 1 (홀수)인 날에는 1이자라고\n            // i % 2 ==0 (짝수)인 날에는 2가 자란다.\n            // 모든 나무의 키가 maxTree와 높이가 같아져야한다.\n            // diff 배열을 만들어야?\n            // ! 아래 방식은 전체 일정 공유를 생각 안하고 만든것.\n            int [] diff = new int [N];\n\n//            for(int i = 0; i < N; i++){\n//                diff[i] = maxTree - trees[i];\n//                if (diff[i] == 0){\n//                    continue;\n//                }else if(diff[i] == 1){\n//                    diff[i] = 1;\n//                }else if(diff[i] == 2){\n//                    diff[i] = 2;\n//                }else if(diff[i] % 3 == 0){\n//                    diff[i] = diff[i] / 3 * 2;\n//                }else if(diff[i] % 3 == 1){\n//                    diff[i] = diff[i] / 3 * 2 + 1;\n//                }else if(diff[i] % 3 == 2){\n//                    diff[i] = diff[i] / 3 * 2 + 2;\n//                }\n//            }\n//            int answer = 0;\n//            for(int i = 0; i < N; i++){\n//                answer += diff[i];\n//            }\n\n            // 나무별 소요일을 더하는 방식이 아닌, 작업별로 쪼개고 합쳐야한다.\n            // 키가 1 자라는 홀수번 째 날(answer = oneCount * 2 - 1)\n            // 1은 한묶음(이틀)에서 하루만 쓰므로\n            int oneCount = 0;\n            // 키가 2 자라는 짝수번 째 날(answer = twoCount * 2)\n            // 2는 한묶음(이틀)\n            int twoCount = 0;\n\n            for(int i = 0 ; i < N ; i++){\n                diff[i] = maxTree - trees[i];\n                oneCount += diff[i] % 2;\n                twoCount += diff[i] / 2;\n            }\n\n            // (중요!) 만약 twoCount짜리가 oneCount보다 2개 이상 많다면,\n            // twoCount를 하나 줄이고 oneCount를 두개로 치환한다.\n            while(twoCount > oneCount + 1){\n                twoCount -- ;\n                oneCount += 2;\n            }\n\n            int answer = 0;\n\n            // 홀수 일이 더 많다면,\n            if(oneCount > twoCount){\n                answer = oneCount * 2 - 1;\n            }\n            // 짝수 일이 같거나 더 많다면,\n            else{\n                answer = twoCount * 2;\n            }\n            sb.append('#').append(test_case).append(' ').append(answer);\n            System.out.println(sb);\n        }\n    }\n}",
  "tree:minkyung": "import java.util.*;\nimport java.io.*;\n\nclass Solution\n{\n\tpublic static void main(String args[]) throws Exception\n\t{\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        int T = Integer.parseInt(br.readLine());\n        StringBuilder sb = new StringBuilder();\n        for (int t = 0; t < T; t++) {\n            int N = Integer.parseInt(br.readLine());\n            int[] trees = new int[N];\n            StringTokenizer st = new StringTokenizer(br.readLine());\n            int maxHeight = 0;\n            for (int i = 0; i < N; i++) {\n                trees[i] = Integer.parseInt(st.nextToken());\n                if (trees[i] > maxHeight) {\n                \tmaxHeight = trees[i];\n                }\n            }\n            \n            int[] remained = new int[N];\n            // 물을 줘야 할 높이 계산 \n            for (int i = 0; i < N; i++) {\n                remained[i] = maxHeight - trees[i];\n            }\n            \n            int dayCount = 0;\n            int twoCount = 0;\n            int oneCount = 0;\n            \n            for (int i = 0; i < N; i++) {\n            \tif (remained[i] > 0) {\n            \t\ttwoCount += remained[i] / 2;\n            \t\toneCount += remained[i] % 2;\n            \t}\n            }\n             \n            while (oneCount + 1 < twoCount) {\n            \ttwoCount -= 1;\n            \toneCount += 2;\n            }\n            \n            if (twoCount >= oneCount) {\n            \tdayCount = twoCount * 2;\n            }\n            else {\n            \tdayCount = oneCount * 2 - 1;\n            }\n            \n            sb.append(\"#\" + (t+1) + \" \" + dayCount).append(\"\\n\");\n            \n        }\n        \n        System.out.println(sb.toString());\n    }\n    \n}",
  "menu:minkyung": "import java.util.*;\n\nclass Solution {\n    static Map<String, Integer> selected;\n    static StringBuilder sb = new StringBuilder();\n    \n    public String[] solution(String[] orders, int[] course) {\n        List<String> answer = new ArrayList<>();\n        selected = new HashMap<>();\n        \n        for (String order: orders) {\n            for (int i = 2; i <= order.length(); i++) {\n                getCombinations(0, 0, i, order);\n            }\n        }\n        \n        for (int c: course) {\n            int max = 0;\n            List<Map.Entry<String, Integer>> list = new ArrayList<>();\n            for (Map.Entry<String, Integer> entry: selected.entrySet()) {\n                if (entry.getKey().length() == c && entry.getValue() >= 2) {\n                    // 목표한 단품 메뉴인 경우 \n                    list.add(entry);\n                }\n            }\n            \n            list.sort((a, b) -> Integer.compare(b.getValue(), a.getValue()));\n            \n            if (list.size() > 0) {\n                int maxValue = list.get(0).getValue();\n                for (Map.Entry<String, Integer> entry: list) {\n                    if (entry.getValue() == maxValue) {\n                        answer.add(entry.getKey());\n                    }\n                }\n            }\n        }\n        \n        answer.sort((a, b) -> a.compareTo(b));\n        \n        return answer.toArray(new String[0]);\n    }\n    \n    public void getCombinations(int index, int depth, int object, String order) {\n        if (depth == object) {\n            char[] arr = sb.toString().toCharArray();\n            Arrays.sort(arr);\n            \n            String result = new String(arr);\n            selected.put(result, selected.getOrDefault(result, 0) + 1);\n            return;\n        }\n    \n        for (int i = index; i < order.length(); i++) {\n            sb.append(order.charAt(i));\n            getCombinations(i+1, depth+1, object, order);\n            sb.deleteCharAt(sb.length() - 1);\n        }\n    }\n}",
  "menu:yeajeong": "import java.util.*;\n\nclass Solution {     \n    public String[] solution(String[] orders, int[] course) {\n    //최소 두명 이상의 손님의 코스요리 메뉴\n    //조합 depth 최대 10 \n    //coused는 오름차순\n    //배열의 원소의 저장된 문자열 오름차순 필요.\n    //메뉴로 조합생성 -> map에 저장 -> 카운팅 -> course 반복문 돌려서 1위찾기\n    String [] answer = {};\n    List<String> newMenus = new ArrayList<>();\n    \n   \n        \n        for (int crs : course){\n                 Map<String, Integer> map = new HashMap<String, Integer>();\n            int max = 0;\n            for (String order : orders){\n\n                char[] chars = order.toCharArray();\n                Arrays.sort(chars);\n                String sortedOrder = new String(chars);\n                comb(0, crs, sortedOrder, \"\", map);\n                }\n        \n            for(Map.Entry<String, Integer> entry : map.entrySet()){\n                String menu = entry.getKey();\n                int count = entry.getValue();\n                \n                max = Math.max(count, max);\n            }\n            \n            \n            for(Map.Entry<String, Integer> entry : map.entrySet()){\n                String menu = entry.getKey();\n                int count = entry.getValue();\n                \n                if(count >=2 && count >= max){\n                newMenus.add(menu);\n                }            \n            }\n        \n        }\n    \n        \n        Collections.sort(newMenus);\n        \n        \n        return newMenus.toArray(new String[0]);\n    \n    }\n    //조합 코드 짜기\n    \n    static void comb(int start, int cnt, String str, String result, Map<String, Integer>map){\n        \n        if(result.length() == cnt){\n            map.put(result, map.getOrDefault(result,0)+1);\n            return;\n        }\n        \n        for( int i = start ; i < str.length() ; i++ ){\n            char ch = str.charAt(i);\n            \n            comb(i+1, cnt, str, result+ch, map);\n        }\n        \n    }\n}",
  "menu:taekgi": "iimport java.util.*;\n\nclass Solution {\n    \n    static Map<String, Integer> menuCombo = new HashMap<>(); // 모든 주문의 조합 빈도수\n\n    // temp: 현재 조합, order: idx에 대응하는 주문\n    static void dfs(int idx, String tmp, String order) {\n        if (tmp.length() > order.length())\n            return;\n\n        menuCombo.put(tmp, menuCombo.getOrDefault(tmp, 0) + 1); // 해당 메뉴의 빈도수 증가\n\n        for (int i = idx; i < order.length(); i++) {\n            dfs(i + 1, tmp + order.charAt(i), order); // 조합 확장 ex.A -> AB -> ABC\n        }\n    }\n\n    public String[] solution(String[] orders, int[] course) {\n        List<String> answer = new ArrayList<>();\n\n        // 주문별 정렬한 후 조합 생성\n        for (String order : orders) {\n            char[] chars = order.toCharArray(); // 문자로 변경 후 정렬해야 함(AB와 BA를 동일시로 만들어야 함)\n            Arrays.sort(chars); // 주문별 정렬\n            order = new String(chars);\n\n            dfs(0, \"\", order); // 조합 가능한 메뉴들을 m에 추가\n        }\n\n        // 해당 단품메뉴의 개수에서 많이 선택된 메뉴 찾기\n        for (int setSize : course) {\n\n            // 많이 선택된 메뉴 횟수 저장\n            int maxOrder = 0;\n\n            for (Map.Entry<String, Integer> menu : menuCombo.entrySet()) {\n                if (menu.getKey().length() == setSize) { // 원하는 단품 메뉴 개수가 나오면\n                    maxOrder = Math.max(maxOrder, menu.getValue()); // 가장 많이 선택된 코스 저장\n                }\n            }\n\n            if (maxOrder <= 1)\n                continue; // 최소 2번 주문 이상이 되어야 함\n\n            for (Map.Entry<String, Integer> menu : menuCombo.entrySet()) {\n                // 해당 단품메뉴가 단품메뉴 개수와 동일하고 많이 주문된 메뉴라면\n                if (menu.getKey().length() == setSize\n                        && menu.getValue() == maxOrder) {\n\n                    answer.add(menu.getKey());\n                }\n            }\n        }\n\n        Collections.sort(answer); // 오름차순\n\n        return answer.toArray(new String[0]);\n    }\n}",
  "menu:jonghyuck": "import java.util.*;\nimport java.io.*;\n\nclass Solution {\n    static Map <String, Integer> map = new HashMap<>();\n    public String[] solution(String[] orders, int[] course) {\n\n\n        // String배열을 char배열로 변환하고, 조합을 map에 추가한다.\n        for(String order : orders){\n            char [] charArray = order.toCharArray();\n            Arrays.sort(charArray);\n            for(int r : course){\n                combinations(charArray, 0, charArray.length, r, \"\");\n            }\n\n        }\n\n//       System.out.println(map);\n//        String[] answer = new String [course.length];\n\n        // course 하나당 여러개 menu가 올수 있으므로 List 선언\n        List <String> answerList = new ArrayList<>();\n        // map 순회\n        // 가장많이, 최소 2명 이상\n        // key를 저장하는 set\n        Set<String> keySet = map.keySet();\n\n        // 각 course의 길이마다, 최대로 많이 나온 빈도수를 구한다.\n        for(int i = 0; i < course.length ; i++){\n            // 빈도수 최댓값\n            int maxValue = 0;\n            for(String key : keySet){\n                String menu = key;\n                int count = map.get(key);\n                if(menu.length() == course[i] && count >= 2 && count > maxValue){\n                    maxValue = count;\n                }\n\n            }\n            // 빈도수가 가장 많고, 2명 이상 고른 메뉴를 answerList에 add\n            for(String key : keySet){\n                String menu = key;\n                int count = map.get(key);\n                if(menu.length() == course[i] && count == maxValue && count >=2){\n                    answerList.add(menu);\n                }\n            }\n\n        }\n        // 오름차순 정렬\n        Collections.sort(answerList);\n\n        // return타입이 String[]이므로, String 배열로 변환\n        return answerList.toArray(new String[answerList.size()]);\n    }\n\n    // 메뉴의 조합을 map에 추가하는 함수 <조합, 빈도수>\n    public void combinations(char[] arr, int start, int n, int r, String result){\n        if(r == 0){\n\t\t        // 동일한 메뉴가 이미 있다면 빈도수 +  1\n            if (map.containsKey(result)) {\n                map.put(result, map.get(result) + 1);\n            } else {\n                map.put(result, 1);\n            }\n            return;\n        }else{\n            for(int i = start; i < n; i++){\n                combinations(arr, i + 1, n, r - 1, result + arr[i]);\n\n            }\n        }\n\n    }\n}",
  "height:jonghyuck": "package week4;\n\nimport java.util.*;\nimport java.io.*;\n\n\nclass SWEA_키순서\n{\n    static List<Integer>[] graph;\n    static List<Integer>[] reverseGraph;\n    static boolean[] visited;\n    static int N;\n    static int M;\n    static int count;\n    public static void main(String args[]) throws Exception\n    {\n        // 해당 학생보다 작은 친구 + 큰친구 = N-1이면 해당 학생은 순서를 아는것\n        //\n\n        // System.setIn(new FileInputStream(\"jonghyuck/src/week4/sample_input.txt\"));\n\n\t\t/*\n\t\t   표준입력 System.in 으로부터 스캐너를 만들어 데이터를 읽어옵니다.\n\t\t */\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n\n        int T = Integer.parseInt(br.readLine());\n\n        for(int test_case = 1; test_case <= T; test_case++)\n        {\n            N = Integer.parseInt(br.readLine());\n            M = Integer.parseInt(br.readLine());\n\n            graph = new ArrayList [N+1];\n            reverseGraph = new ArrayList[N+1];\n\n\n\n            for(int i = 1; i <= N; i++){\n                graph[i] = new ArrayList<>();\n                reverseGraph[i] = new ArrayList<>();\n            }\n\n            int answer = 0;\n\n            for(int i = 0; i < M; i++ ){\n                StringTokenizer st = new StringTokenizer(br.readLine());\n\n                int a = Integer.parseInt(st.nextToken());\n                int b = Integer.parseInt(st.nextToken());\n\n                graph[a].add(b);\n                reverseGraph[b].add(a);\n\n            }\n\n            for(int i = 1; i <= N ; i++){\n                visited = new boolean[N + 1];\n                count = 0;\n                dfs(i, graph);\n                int taller = count;\n\n                visited = new boolean[N + 1];\n                count = 0;\n                dfs(i, reverseGraph);\n                int shorter = count;\n\n                if(taller + shorter == N - 1){\n                    answer ++;\n                }\n            }\n            StringBuilder sb = new StringBuilder();\n\n\n            sb.append('#').append(test_case).append(' ').append(answer);\n\n            System.out.println(sb);\n\n\n        }\n    }\n\n    static void dfs(int current, List<Integer>[] graph){\n        visited[current] = true;\n        for(int next : graph[current]){\n\n            if(!visited[next]){\n                visited[next] = true;\n                count ++;\n\n\n                dfs(next, graph);\n\n            }\n        }\n\n    }\n}",
  "height:yeajeong": "package codingtest;\n\nimport java.io.BufferedReader;\nimport java.io.IOException;\nimport java.io.InputStreamReader;\nimport java.util.ArrayDeque;\nimport java.util.Arrays;\nimport java.util.Queue;\nimport java.util.StringTokenizer;\n\npublic class Solution {\n\n\tstatic boolean[][] graph;\n\n\tpublic static void main(String[] args) throws Exception {\n\n\t\tBufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n\t\tStringTokenizer st = new StringTokenizer(br.readLine());\n\t\tStringBuilder sb = new StringBuilder();\n\n\t\tint test_case = Integer.parseInt(st.nextToken());\n\n\t\tfor (int tc = 0; tc < test_case; tc++) {\n\t\t\t\n\t\t\tsb.append(\"#\").append(tc + 1).append(\" \");\n\n\t\t\tint N = Integer.parseInt(new StringTokenizer(br.readLine()).nextToken());\n\t\t\tint M = Integer.parseInt(new StringTokenizer(br.readLine()).nextToken());\n\t\t\tint peopleCount = 0 ;\n\t\t\t\n\t\t\tgraph = new boolean[N + 1][N + 1];\n\n\t\t\tfor (int i = 0; i < M; i++) {\n\n\t\t\t\tst = new StringTokenizer(br.readLine());\n\n\t\t\t\tint start = Integer.parseInt(st.nextToken());\n\t\t\t\tint end = Integer.parseInt(st.nextToken());\n\n\t\t\t\tgraph[start][end] = true;\n\n\t\t\t}\n\t\t\t\n\t\t\t\n\t\t\tfor(int i = 1 ; i <= N ;i++) {\n\t\t\t\tint smallCount = countBFS(i, N , false);\n\t\t\t\tint biggerCount = countBFS(i,N, true);\n\t\t\t\tint sum = smallCount + biggerCount;\n\t\t\n\t\t\t\tif((sum)== N-1) {\n\t\t\t\t\tpeopleCount++;\n\t\t\t\t}\n\t\t\t\n\t\t\t}\n\n\t\t\tsb.append(peopleCount).append(\"\\n\");\n\t\t}\n\t\tSystem.out.println(sb);\n\t}\n\n\tstatic int countBFS(int me, int N, boolean findBigger) {\n\n\t\tQueue<Integer> q = new ArrayDeque<Integer>();\n\t\tboolean[] visited = new boolean[N+1];\n\t\t\n\t\tq.offer(me);\n\t\tvisited[me] = true; \n\t\t\n\t\tint count =0; \n\t\t\n\t\twhile (!q.isEmpty()) {\n\t\t\t\n\t\t\tint cur = q.poll();\n\t\t\t\n\t\t\tfor (int i = 1; i <= N; i++) {\n\t\t\t\tboolean connected;\n\t\t\t\t\n\t\t\t\tif (findBigger){\n\t\t\t\t\tconnected = graph[cur][i];\n\t\t\t\t}else {\n\t\t\t\t\tconnected = graph[i][cur];\n\t\t\t\t}\n\t\t\t\t\n\t\t\t\tif(connected && !visited[i]) {\n\t\t\t\t\tq.offer(i);\n\t\t\t\t\tvisited[i] = true;\n\t\t\t\t\tcount++;\n\t\t\t\t}\n\n\t\t\t}\n\t\t}\n\n\t\treturn count;\n\t}\n\t\n}",
  "height:taekgi": "import java.util.*;\nimport java.io.*;\n\nclass Solution {\n    static ArrayList<Integer>[] adj; // 정방향 노드 (작은 것 -> 큰 것)\n    static ArrayList<Integer>[] revAdj; // 역방향 노드 (큰 것 -> 작은 것)\n    static int N, M;\n    \n\tpublic static void main(String args[]) throws Exception {\n\t\tBufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringBuilder sb = new StringBuilder();\n        \n\t\tint T = Integer.parseInt(br.readLine());\n\n\t\tfor(int tc = 1; tc <= T; tc++) {\n            N = Integer.parseInt(br.readLine()); // 학생들의 수\n            M = Integer.parseInt(br.readLine()); // 비교한 횟수\n\t\t\t\n            // adj와 revAdj 초기화\n            adj = new ArrayList[N+1]; // 크기 지정\n            revAdj = new ArrayList[N+1];\n            for(int i = 1; i <= N; i++) {\n                adj[i] = new ArrayList<>();\n                revAdj[i] = new ArrayList<>();\n            }\n            \n            // a b 입력받은 후 연관관계 만들기\n            for (int i = 0; i < M; i++) {\n                StringTokenizer st = new StringTokenizer(br.readLine());\n                int from = Integer.parseInt(st.nextToken());\n                int to = Integer.parseInt(st.nextToken());\n                \n                adj[from].add(to);\n                revAdj[to].add(from);\n            }\n            \n            // 자신보다 작은 사람과 자신보다 큰 사람의 합이 전체 인원에서 -1한 값이 동일한지 확인\n            int answer = 0;\n            for(int i = 1; i <= N; i++) {\n                if(bfs(i, adj) + bfs(i, revAdj) == N - 1) {\n                    answer++;\n                }\n            }\n            sb.append(\"#\").append(tc).append(\" \").append(answer).append(\"\\n\");\n        }\n \t\tSystem.out.print(sb);       \n\t}\n    \n    // start에서 방문할 수 있는 노드의 수 출력\n    static int bfs(int start, ArrayList<Integer>[] graph) {\n        Queue<Integer> queue = new ArrayDeque<>();\n        boolean[] visited = new boolean[N+1]; // 각 노드별 방문 여부\n        queue.add(start);\n        visited[start] = true;\n        \n        int count = 0; // 방문 노드 수\n        while(!queue.isEmpty()) {\n            int curr = queue.poll();\n            for(int next : graph[curr]) { // next : 현재 노드와 연결된 노드\n                if(!visited[next]) {\n                    visited[next] = true; // 다음 노드 방문\n                    queue.add(next); // 그 다음 노드 방문을 위해 큐에 추가\n                    count++; // 방문 노드 수 +1\n                }\n            }\n        }\n        \n        return count;\n    }\n}",
  "height:minkyung": "import java.util.*;\nimport java.io.*;\n\npublic class Solution {\n    static int[] visited;\n    static List<Integer>[] graph;\n    static List<Integer>[] reverseGraph;\n    \n\tpublic static void main(String[] args) throws IOException {\n\t\tBufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n\t\tint T = Integer.parseInt(br.readLine());\n\t\tStringBuilder sb = new StringBuilder();\n\t\t\n\t\tfor (int t = 0; t < T; t++) {\n\t\t\tint N = Integer.parseInt(br.readLine());\n\t\t\tint M = Integer.parseInt(br.readLine());\n\t\t\tvisited = new int[N+1];\n            graph = new ArrayList[N+1];\n            reverseGraph = new ArrayList[N+1];\n \n\t\t\tfor (int i = 1; i <= N; i++) {\n\t\t\t\tgraph[i] = new ArrayList<>();\n\t\t\t\treverseGraph[i] = new ArrayList<>();\n\t\t\t}\n\t\t\t\n\t\t\t// 그래프 만들기 \n\t\t\tfor (int i = 0; i < M; i++) {\n\t\t\t\tStringTokenizer st = new StringTokenizer(br.readLine());\n\t\t\t\tint a = Integer.parseInt(st.nextToken());\n\t\t\t\tint b = Integer.parseInt(st.nextToken());\n\t\t\t\t\n\t\t\t\tgraph[a].add(b);\n\t\t\t\treverseGraph[b].add(a);\n\t\t\t\t\n\t\t\t}\n\t\t\t\n\t\t\tint answer = 0;\n\t\t\tfor (int i = 1; i <= N; i++) {\n\t\t\t\tint front = bfs(graph, i, N);\n\t\t\t\tint behind = bfs(reverseGraph, i, N);\n\t\t\t\t\n\t\t\t\tif (front + behind == N - 1) {\n\t\t\t\t\tanswer++;\n\t\t\t\t}\n\t\t\t}\n\t\t\t\n\t\t\tsb.append(\"#\").append(t+1).append(\" \").append(answer).append(\"\\n\");\n\t\t\t\n\t\t}\n\t\t\n\t\tSystem.out.println(sb.toString());\n\t}\n\t\n\tpublic static int bfs(List<Integer>[] graph, int start, int N) {\n\t\tint count = 0;\n        visited = new int[N+1];\n\t\tDeque<Integer> dq = new ArrayDeque<>();\n\t\tdq.offerLast(start);\n\t\tvisited[start] = 1;\n        \n\t\twhile (!dq.isEmpty()) {\n\t\t\tint next = dq.pollFirst();\n\t\t\tcount++;\n\t\t\t\n\t\t\tList<Integer> nodes = graph[next];\n\t\t\tfor (int node: nodes) {\n\t\t\t\tif (visited[node] == 0) {\n\t\t\t\t\tvisited[node] = 1;\n\t\t\t\t\tdq.offerLast(node);\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t\treturn count-1;\n\t}\n}",
  "supply:jonghyuck": "package week4;\n\n\nimport java.io.*;\nimport java.util.*;\n\n\nclass SWEA_보급로_dijkstra\n{\n    static int [] dx = {-1, 0, 1, 0};\n    static int [] dy = {0, 1, 0 ,-1};\n    static int [][] map;\n    static int [][] costs;\n    static int N;\n    static int time;\n\n    public static void main(String args[]) throws Exception\n    {\n\n        System.setIn(new FileInputStream(\"jonghyuck/src/week4/input.txt\"));\n\n\t\t/*\n\t\t   표준입력 System.in 으로부터 스캐너를 만들어 데이터를 읽어옵니다.\n\t\t */\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n\n        int T = Integer.parseInt(br.readLine());\n\t\t/*\n\t\t   여러 개의 테스트 케이스가 주어지므로, 각각을 처리합니다.\n\t\t*/\n\n        for(int test_case = 1; test_case <= T; test_case++)\n        {\n            N = Integer.parseInt(br.readLine());\n            map = new int[N][N];\n            costs = new int[N][N];\n            time = 0;\n//           for(int row = 0; row < N; row ++){\n//               StringTokenizer st = new StringTokenizer(br.readLine());\n//               for(int col = 0; col < N; col++){\n//                   map[row][col] = Integer.parseInt(st.nextToken());\n//\n//               }\n//           }\n            // 문자열로 받아서 쪼개고 형변환 해야함\n            for(int row = 0; row < N; row++){\n                String line = br.readLine();\n                for(int col = 0; col < N; col++){\n                    map[row][col] = line.charAt(col) - '0';\n                }\n            }\n            StringBuilder sb = new StringBuilder();\n\n            int answer = dijkstra(0, 0);\n\n            sb.append('#').append(test_case).append(' ').append(answer);\n\n            System.out.println(sb);\n\n        }\n    }\n    static int dijkstra(int startRow, int startCol){\n        PriorityQueue <int []> queue = new PriorityQueue<>((a,b) -> a[2]-b[2]);\n        queue.offer(new int[]{0, 0, 0});\n        for(int i = 0; i < N; i++){\n            Arrays.fill(costs[i], Integer.MAX_VALUE);\n        }\n        costs[startRow][startCol] = 0;\n\n        while(!queue.isEmpty()){\n            int [] current = queue.poll();\n            int currentRow = current[0];\n            int currentCol = current[1];\n            int currentCost = current[2];\n\n            if(currentCost > costs[currentRow][currentCol]) {\n                continue;\n            }\n            if(currentRow == N-1 && currentCol == N-1){\n                return currentCost;\n            }\n\n            for(int direction = 0; direction < 4; direction ++){\n\n                int nextRow = currentRow + dx[direction];\n                int nextCol = currentCol + dy[direction];\n\n                if(nextRow < 0 || nextRow >= N || nextCol < 0 || nextCol >= N){\n                    continue;\n                }\n                int nextCost = currentCost + map[nextRow][nextCol];\n\n                if(nextCost < costs[nextRow][nextCol]){\n\n                    costs[nextRow][nextCol] = nextCost;\n\n                    queue.offer(new int [] {nextRow, nextCol, nextCost});\n\n\n                }\n\n\n            }\n\n\n        }return costs[N-1][N-1];\n    }\n}",
  "supply:yeajeong": "package codingtest;\n\nimport java.io.BufferedReader;\nimport java.io.IOException;\nimport java.io.InputStreamReader;\nimport java.util.*;\n\n// 그래프인건 알겠는데, 어떻게 자신이 몇번째인지 알 수 있는 노드인지 모르겠다.\npublic class Solution {\n\n\n\tstatic int[][] map;\n\tstatic int N;\n\tstatic int resultCost;\n\tstatic int[] dr = { -1, 1, 0, 0 };\n\tstatic int[] dc = { 0, 0, -1, 1 };\n\tstatic int[][] costMap;\n\n\tpublic static void main(String[] args) throws Exception {\n\n\t\tBufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n\t\tStringTokenizer st;\n\t\tStringBuilder sb = new StringBuilder();\n\t\tint test_case = Integer.parseInt(br.readLine());\n\n\t\tfor (int tc = 1; tc <= test_case; tc++) {\n\t\t\tN = Integer.parseInt(br.readLine());\n\t\t\tmap = new int[N][N];\n\t\t\tcostMap = new int[N][N];\n\t\t\tresultCost = Integer.MAX_VALUE;\n\t\t\tsb.append(\"#\"+tc+\" \");\n\t\t\t\n\t\t\tfor (int i = 0; i < N; i++) {\n\t\t\t\tString s = br.readLine();\n\t\t\t\tfor (int j = 0; j < N; j++) {\n\t\t\t\t\tmap[i][j] = s.charAt(j) - '0';\n\t\t\t\t}\n\t\t\t\tArrays.fill(costMap[i], Integer.MAX_VALUE);\n\t\t\t}\n\t\t\t// 입력받기\n\n\t\t\tQueue<int[]> q = new ArrayDeque<>();\n\t\t\tq.offer(new int[] { 0, 0 });\n\t\t\tcostMap[0][0] = 0;\n\t\t\tfindMinCost(costMap, q);\n\t\t\tsb.append(resultCost).append(\"\\n\");\n\t\t\t\n\t\t\t\n\t\t}\n\t\tSystem.out.println(sb);\n\n\t\t// BFS코드\n\n\t}\n\n\tstatic void findMinCost(int[][] costMap, Queue<int[]> q) {\n\n\t\twhile (!q.isEmpty()) {\n\t\t\tint[] current = q.poll();\n\t\t\tint cr = current[0];\n\t\t\tint cc = current[1];\n\t\t\tint currentCost = costMap[cr][cc];\n\n\t\t\tif (currentCost >= resultCost)\n\t\t\t\tcontinue;\n\t\t\t// base 조건\n\t\t\tif (cr == N - 1 && cc == N - 1) {\n\t\t\t\tif (currentCost < resultCost) {\n\t\t\t\t\tresultCost = currentCost;\n\t\t\t\t}\n\t\t\t\tcontinue;\n\t\t\t}\n\n\t\t\tfor (int d = 0; d < 4; d++) {\n\n\t\t\t\tint nr = cr + dr[d];\n\t\t\t\tint nc = cc + dc[d];\n\n\t\t\t\tif (!isRange(nr, nc))\n\t\t\t\t\tcontinue;\n\t\t\t\t// \n\t\t\t\tint newCost = currentCost + map[nr][nc];\n\t\t\t\tif (newCost < costMap[nr][nc]) {\n\t\t\t\t\tcostMap[nr][nc] = newCost;\n\t\t\t\t\tq.offer(new int[] { nr, nc });\n\t\t\t\t}\n\n\t\t\t}\n\n\t\t} // end while\n\n\t}\n\n\tstatic boolean isRange(int nr, int nc) {\n\n\t\tif (nr < 0 || nr > N - 1 || nc < 0 || nc > N - 1)\n\t\t\treturn false;\n\t\treturn true;\n\t}\n\n}",
  "supply:taekgi": "// SWEA 보급로 문\nimport java.util.*;\nimport java.io.*;\n\n/*\n   각 칸까지 도달하는 최소 sum을 구하고 마지막에 최솟값 갱신\n */\nclass Solution {\n\tstatic int ans, N; // 지도의 크기\n\t\n\tstatic int[][] sum; // 합산\n\tstatic int[][] map; // 지도\n\tstatic boolean[][] visited; // 칸별 방문 여부\n\n\t// 상 하 좌 우\n\tstatic int[] dr = { -1, 1, 0, 0 };\n\tstatic int[] dc = { 0, 0, -1, 1 };\n\n\t\tpublic static void main(String args[]) throws Exception {\n\t\tBufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n\t\tStringBuilder sb = new StringBuilder();\n\n\t\tint T = Integer.parseInt(br.readLine());\n\n\t\tfor (int tc = 1; tc <= T; tc++) {\n\t\t\tN = Integer.parseInt(br.readLine()); // 지도의 크기\n\t\t\tans = Integer.MAX_VALUE;\n\n\t\t\tvisited = new boolean[N][N];\n\t\t\tsum = new int[N][N];\n\t\t\tfor (int i = 0; i < N; i++) {\n\t\t\t\tArrays.fill(sum[i], Integer.MAX_VALUE);\n\t\t\t}\n\n\t\t\tmap = new int[N][N];\n\t\t\tfor (int r = 0; r < N; r++) {\n\t\t\t\tString line = br.readLine();\n\t\t\t\tfor (int c = 0; c < N; c++) {\n\t\t\t\t\tmap[r][c] = line.charAt(c) - '0';\n\t\t\t\t}\n\t\t\t}\n\n\t\t\tsum[0][0] = 0;\n\t\t\tvisited[0][0] = true; // 출발지 방문\n\t\t\tdfs(0, 0);\n\t\t\t\n\t\t\tsb.append(\"#\").append(tc).append(\" \").append(ans).append(\"\\n\");\n\t\t}\n\t\t\n\t\tSystem.out.print(sb);\n\t}\n\n\tstatic void dfs(int x, int y) {\n\t\t// 도착지점에 왔을 경우\n\t\tif (x == N - 1 && y == N - 1) {\n\t\t\tans = Math.min(ans, sum[x][y]);\n\n\t\t\treturn;\n\t\t}\n\n\t\tif (ans <= sum[x][y])\n\t\t\treturn;\n\n\t\tfor (int d = 0; d < 4; d++) {\n\t\t\tint nx = x + dr[d];\n\t\t\tint ny = y + dc[d];\n\n\t\t\tif (nx < 0 || nx >= N || ny < 0 || ny >= N) {\n\t\t\t\tcontinue;\n\t\t\t}\n\n\t\t\t// 현재까지 합산에서 현재 위치한 값을 더해서 tmp에 저장\n\t\t\tint tmp = sum[x][y] + map[nx][ny];\n\n\t\t\t// 이동한 칸이 방문하지 않은 칸이거나 초기에 지정한 합산(sum[nx][ny])값보다 작을 경우\n\t\t\tif (!visited[nx][ny] || tmp < sum[nx][ny]) {\n\t\t\t\tvisited[x][y] = true; // 방문 처리\n\t\t\t\tsum[nx][ny] = tmp; // 최솟값 갱신\n\t\t\t\tdfs(nx, ny);\n\t\t\t}\n\t\t}\n\t}\n\n\t// 배열 범위 혹은 방문 여부 혹은 칸의 공사 여부\n\tboolean isRange(int x, int y) {\n\t\tif (x < 0 || x >= N || y < 0 || y >= N || visited[x][y] == true || map[x][y] == 0) {\n\t\t\treturn false;\n\t\t} else {\n\t\t\treturn true;\n\t\t}\n\t}\n}",
  "supply:minkyung": "import java.util.*;\nimport java.io.*;\n\npublic class Solution {\n    static int[][] map;\n    static int[][] visited;\n\n    public static void main(String[] args) throws IOException {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        int T = Integer.parseInt(br.readLine());\n        StringBuilder sb = new StringBuilder();\n\n        for (int t = 0; t < T; t++) {\n            int N = Integer.parseInt(br.readLine());\n            map = new int[N][N];\n            visited = new int[N][N];\n            for (int i = 0; i < N; i++) {\n                Arrays.fill(visited[i], Integer.MAX_VALUE);\n            }\n\n            for (int i = 0; i < N; i++) {\n                String s = br.readLine();\n\n                for (int j = 0; j < N; j++) {\n                    map[i][j] = s.charAt(j) - '0';\n                }\n            }\n\n            bfs(N);\n\n            sb.append(\"#\" + (t+1) + \" \").append(visited[N-1][N-1] - 1).append(\"\\n\");\n\n        }\n\n        System.out.println(sb.toString());\n    }\n\n\n    public static void bfs(int N) {\n        int[] dy = new int[] {0, 1, -1, 0};\n        int[] dx = new int[] {1, 0 , 0, -1};\n\n        Deque<int[]> dq = new ArrayDeque<>();\n        dq.offerLast(new int[] {0, 0});\n        visited[0][0] = 1;\n\n        while (!dq.isEmpty()) {\n            int[] cur = dq.pollFirst();\n            int cy = cur[0];\n            int cx = cur[1];\n\n            for (int i = 0; i < 4 ; i++) {\n                int ny = cy + dy[i];\n                int nx = cx + dx[i];\n\n                if (ny >= 0 && nx >= 0 && ny < N && nx < N) {\n                    if (visited[ny][nx] > visited[cy][cx] + map[ny][nx]) {\n                        visited[ny][nx] = visited[cy][cx] + map[ny][nx];\n                        // 복구 \n\n                        dq.offerLast(new int[] {ny, nx});\n                    }\n                }\n            }\n        }\n    }\n\n}",
  "brick:jonghyuck": "package week5;\n\nimport java.util.*;\nimport java.io.*;\n\nclass SWEA_벽돌깨기\n{\n    static int N;\n    static int W;\n    static int H;\n    static int map[][];\n    static boolean visited [][];\n    // 상하좌우\n    static int dx [] = {0, 0, -1, 1};\n    static int dy [] = {-1, 1, 0, 0};\n    public static void main(String args[]) throws Exception\n    {\n\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n\n        int T = Integer.parseInt(br.readLine());\n        //System.setIn(new FileInputStream(\"res/input.txt\"));\n\n\t\t/*\n\t\t   표준입력 System.in 으로부터 스캐너를 만들어 데이터를 읽어옵니다.\n\t\t */\n\t\t/*\n\t\t   여러 개의 테스트 케이스가 주어지므로, 각각을 처리합니다.\n\t\t*/\n\n\n\n        for(int test_case = 1; test_case <= T; test_case++)\n        {\n            StringTokenizer st = new StringTokenizer(br.readLine());\n\n            N = Integer.parseInt(st.nextToken());\n            W = Integer.parseInt(st.nextToken());\n            H = Integer.parseInt(st.nextToken());\n\n            map = new int [H][W];\n            for(int i = 0; i < H; i ++){\n\n                st = new StringTokenizer(br.readLine());\n\n                for(int j = 0; j < W; j++){\n                    map[i][j] = Integer.parseInt(st.nextToken());\n                }\n            }\n\n\n\n        }\n    }\n    // N이 작다 => 기본적으로 DFS\n    // 폭발은 BFS?\n\n    // 블록 깨는 함수\n    static public void breakBlock(int row, int col){\n        Deque<int[]> queue = new ArrayDeque<>();\n\n        int startBoom = map[row][col];\n\n        queue.offer(new int[] {row, col, startBoom});\n\n        map[row][col] = 0;\n        while(!queue.isEmpty()){\n            int current [] = queue.poll();\n            int currentRow = current[0];\n            int currentCol = current[1];\n            int boom = current[2];\n\n            for(int i = 0 ; i < 4; i ++){\n                for(int j = 1; j < boom; j++){\n                    int nextRow = currentRow + dx[i] * j;\n                    int nextCol = currentCol + dy[i] * j;\n\n                    if(nextRow < 0 || nextRow >= H || nextCol < 0 || nextCol >= W){\n                        break;\n                    }\n                    if(map[nextRow][nextCol] == 0){\n                        continue;\n                    }\n                    int nextBoom = map[nextRow][nextCol];\n\n                    map[nextRow][nextCol] = 0;\n\n                    queue.offer(new int[] {nextRow, nextCol, nextBoom});\n                }\n\n\n            }\n        }\n\n\n    }\n\n    static int dfs(int count){\n        int remain = 0;\n        if(count == N){\n            // remain\n            for(int i = 0 ; i < H; i++){\n                for(int j = 0 ; j < W; j++){\n                    if(map[H][W] != 0){\n                       remain ++; \n                    }\n                }\n            }\n            return remain;\n        }\n        for(int col = 0; col < W; col++){\n\n            dfs(count + 1);\n        }\n        return 0;\n    }\n\n    static void move(){\n        \n    }\n\n}",
  "brick:yeajeong": "package codingtest;\n\nimport java.io.BufferedReader;\nimport java.io.InputStreamReader;\nimport java.util.*;\n\npublic class Solution {\n\t/*\n\t * 좁은 n의 범위, 재귀로 풀 수 있을지는 모르겠다. 누적합은 안되고, 자료구조도 맵구조라 이차원 배열을.... 백트래킹, 정도로 풀어보자.\n\t * \n\t * return 최대한 많은 벽돌을 부수는 경우에 벽돌의 개수를 구하라\n\t * \n\t * 단계 1. 벽돌을 떨구고 2. 블록파괴 (벽돌의 연쇄작용 - 다시살리기) 3. 블록내리기 4. 반복\n\t */\n\tstatic int N;\n\tstatic int W;\n\tstatic int H;\n\tstatic int[][] matrix;\n\tstatic int maxScore;\n\tstatic int[] topBlock;\n\n\tpublic static void main(String[] args) throws Exception {\n\t\t// input 받기\n\t\tBufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n\t\tStringTokenizer st;\n\t\tStringBuilder sb = new StringBuilder();\n\n\t\tint T = Integer.parseInt(br.readLine());\n\n\t\tfor (int tc = 0; tc <= T; tc++) {\n\t\t\tst = new StringTokenizer(br.readLine());\n\t\t\tN = Integer.parseInt(st.nextToken());\n\t\t\tW = Integer.parseInt(st.nextToken());\n\t\t\tH = Integer.parseInt(st.nextToken());\n\t\t\ttopBlock = new int[W];\n\t\t\tmatrix = new int[W][H];\n\n\t\t\tfor (int i = 0; i < H; i++) {\n\t\t\t\tst = new StringTokenizer(br.readLine());\n\t\t\t\tfor (int j = 0; j < W; j++) {\n\t\t\t\t\tmatrix[i][j] = Integer.parseInt(st.nextToken());\n\t\t\t\t\tif (topBlock[j] == 0 && matrix[i][j] != 0)\n\t\t\t\t\t\ttopBlock[j] = i;\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// input끝\n\n//\t\t\tdfs\n\n\t\t}\n\t}\n\n\tvoid dfs(int beads, int restBlockCount, int[][] map) {\n\t\t// 종료조건, 구슬의 개수가 끝일때\n\t\tif (beads == N) {\n\t\t\tmaxScore = Math.max(maxScore, restBlockCount);\n\t\t\treturn;\n\t\t}\n\t\t//현재 내 상태 저장\n\t\tint [][] mapStatus = new int[W][H];\n\t\t\n\t\tfor(int i = 0 ; i < N ; i++) {\n\t\t\tmapStatus[i] = map[i].clone();\n\t\t}\n\t\t\n\t\t// 1. 구슬을 떨구고\n\t\tfor (int i = 0; i < W; i++) {\n\t\t\t// 남은 벽돌수 계산해야함\n\t\t\t\n\t\t\t// i번으로 블록이 떨어진다.\n\t\t\tcrashBlock(i, mapStatus);\n\t\t\t\n\t\t\t\n\t\t\tdfs(beads + 1, restBlockCount , mapStatus);\n\n\t\t}\n\n\t\t// 2. 블록파괴 (벽돌의 연쇄작용 - 다시살리기)\n\t\t// 3. 블록내리기\n\t\t// 4. 반복\n\n\t}\n\n\tvoid crashBlock(int col, int[][] mapStatus) {\n\n\t\t// 방향 하 좌 우\n\t\tint[] dr = { 1, 0, 0 };\n\t\tint[] dc = { 0, -1, 1 };\n\n\t\t// 블록을 뿌시자.\n\t\tint row = topBlock[col];\n\t\tint power = mapStatus[row][col];\n\t\tDeque<int[]> q = new ArrayDeque<>();\n\t\tq.offer(new int[] { row, col, power });\n\n\t\t// 내위치부터 내위치 포함 파워까지, 즉 내좌표 ~ power-1까지 이동\n\t\t// 이걸 왼쪽, 오른쪽, 아래로, 중간에 1아닌거있으면 큐에추가\n\t\twhile(!q.isEmpty()) {\n\t\t\tint[] current = q.poll();\n\t\t\tint cr = current[0];\n\t\t\tint cc = current[1];\n\t\t\tint cp = current[2];\n\t\t\t\n\t\t\t//자기 칸 부수기\n\t\t\tmapStatus[cr][cc] = 0;\n\t\t\t//파워해서 뿌순다.\n\t\t\tfor(int d = 0 ; d < 3 ; d++) {\n\t\t\t\tint nr = cr ;\n\t\t\t\tint nc = cc ;\n\t\t\t\tint np = mapStatus[cr][cc];\n\t\t\t\t\n\t\t\t\t//파워만큼 부순다.\n\t\t\t\tfor(int p = 0 ; p < np ; p++) {\n\t\t\t\t\tnr += dr[d]*p;\n\t\t\t\t\tnc += dc[d]*p;\n\t\t\t\t\tnp = mapStatus[nr][nc];\n\t\t\t\t\t\n\t\t\t\t\t//범위 넘으면 continue\n\t\t\t\t\tif(!isRange(nr,nc)) continue;\n\t\t\t\t\t\n\t\t\t\t\tif(np == 1) {//cp가 1이면 자기칸만 부수고 큐에 추가 x\n\t\t\t\t\t\tmapStatus[nr][nc] = 0;\n\t\t\t\t\t\tcontinue;\n\t\t\t\t\t}else if(np > 1){//cp가 1이 아니면 여러칸을 부순다.\n\t\t\t\t\t\tq.offer(new int[] {nr,nc,np});\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t\t\n\t\t\t\n\t\t\t}// 방향 \n\t\t\t\n\t\t}\n\t\t\n\t\t\n\t\t\n\t}//funtion\n\t\n\tboolean isRange(int r, int c ) {\n\t\t\n\t\tif(r < 0 ||  r >=N || c < 0 || c>= N) {\n\t\t\treturn false;\n\t\t}\n\t\treturn true;\n\t\t\n\t}\n\n}",
  "brick:taekgi": "import java.io.*;\nimport java.util.*;\n\n/*\n * 첫 번째 열을 부신다\n * 명중한 벽돌의 상하좌우(벽돌에 적힌 숫자 - 1) 칸 만큼 제거한다\n * 제거된 칸 위에 벽돌이 있으면 중력에 의해 떨어뜨린다\n * \n * N만큼 다 던지면(베이스 조건) 남은 벽돌 개수를 구하고 최솟값인지 아닌지 구한 후 return\n * */\n\npublic class SWEA_5656 {\n\tstatic int N, W, H; // H: 열, W: 행\n\tstatic int ans;\n\n\tstatic int[][] blocks;\n\n\tstatic int[] dr = { -1, 1, 0, 0 };\n\tstatic int[] dc = { 0, 0, -1, 1 };\n\n\tpublic static void main(String args[]) throws Exception {\n\t\tBufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n\t\tStringBuilder sb = new StringBuilder();\n\n\t\tint T = Integer.parseInt(br.readLine());\n\t\tfor (int tc = 1; tc <= T; tc++) {\n\t\t\tStringTokenizer st = new StringTokenizer(br.readLine());\n\n\t\t\tN = Integer.parseInt(st.nextToken()); // 던진 횟수\n\t\t\tW = Integer.parseInt(st.nextToken());\n\t\t\tH = Integer.parseInt(st.nextToken());\n\n\t\t\t// 벽돌에 적힌 값들\n\t\t\tblocks = new int[H][W];\n\t\t\tfor (int r = 0; r < H; r++) {\n\t\t\t\tst = new StringTokenizer(br.readLine());\n\t\t\t\tfor (int c = 0; c < W; c++) {\n\t\t\t\t\tblocks[r][c] = Integer.parseInt(st.nextToken());\n\t\t\t\t}\n\t\t\t}\n\n\t\t\tans = Integer.MAX_VALUE;\n\t\t\tbrokenBlocks(0);\n\n\t\t\tsb.append(\"#\").append(tc).append(\" \").append(ans).append(\"\\n\");\n\t\t}\n\t\tSystem.out.print(sb);\n\t}\n\n\tstatic void brokenBlocks(int count) {\n\t\tif (count == N) {\n\t\t\tint remainBlocks = countBlocks();\n\t\t\tans = Math.min(ans, remainBlocks); // 최솟값\n\n\t\t\treturn;\n\t\t}\n\n\t\t// 모든 열에 구슬을 떨어뜨리기\n\t\tfor (int c = 0; c < W; c++) {\n\t\t\tint[][] copyBlocks = initBlocks(blocks);\n\n\t\t\t// 해당 열에서 떨어뜨릴 가장 위쪽 벽돌 찾기\n\t\t\tfor (int r = 0; r < H; r++) {\n\t\t\t\tif (blocks[r][c] > 0) {\n\t\t\t\t\tdestroy(r, c);\n\n\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// 연쇄 폭발 이후 블록들 떨어뜨리기\n\t\t\tfall();\n\n\t\t\tbrokenBlocks(count + 1);\n\n\t\t\tblocks = copyBlocks;\n\t\t}\n\t}\n\n\t// 벽돌 깨는 로직\n\tstatic void destroy(int r, int c) {\n\t\tDeque<int[]> dq = new ArrayDeque<>();\n\n\t\tdq.offerFirst(new int[] { r, c, blocks[r][c] });\n\t\tblocks[r][c] = 0;\n\t\t\n\t\twhile (!dq.isEmpty()) {\n\t\t\tint[] cur = dq.pollFirst();\n\t\t\tint x = cur[0];\n\t\t\tint y = cur[1];\n\t\t\tint n = cur[2];\n\n\t\t\t// 상하좌우 탐색\n\t\t\tfor (int d = 0; d < 4; d++) {\n\t\t\t\tfor(int i = 1; i < n; i++) {\n\t\t\t\t\t// 행 열 이동\n\t\t\t\t\tint nx = x + dr[d] * i;\n\t\t\t\t\tint ny = y + dc[d] * i;\n\n\t\t\t\t\t// 범위를 벗어나면 여기서 끝내야 함\n\t\t\t\t\tif (!isRange(nx, ny)) {\n\t\t\t\t\t\tbreak;\n\t\t\t\t\t}\n\t\t\t\t\t\n\t\t\t\t\tif(blocks[nx][ny] == 0) {\n\t\t\t\t\t\tcontinue;\n\t\t\t\t\t}\n\n\t\t\t\t\tint nextN = blocks[nx][ny];\n\t\t\t\t\tblocks[nx][ny] = 0; // 벽돌 깨기\n\t\t\t\t\t\n\t\t\t\t\tdq.offer(new int[] {nx, ny, nextN});\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t}\n\n\t// 벽돌 아래로 떨어뜨리기 (연쇄 폭발이 다 끝난 후 진행)\n\tstatic void fall() {\n\t\tfor (int c = 0; c < W; c++) {\n\t\t\tfor (int r = H - 1; r > 0; r--) {\n\t\t\t\t// 0이 발견되면\n\t\t\t\tif (blocks[r][c] == 0) {\n\t\t\t\t\t// 해당 블록 위에 있는 블록들 떨어뜨리기\n\t\t\t\t\tfor (int a = r - 1; a >= 0; a--) {\n\t\t\t\t\t\tif (blocks[a][c] > 0) {\n\t\t\t\t\t\t\tblocks[r][c] = blocks[a][c];\n\n\t\t\t\t\t\t\tblocks[a][c] = 0;\n\t\t\t\t\t\t\tbreak;\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n\n\t// 남은 벽돌들 카운팅\n\tstatic int countBlocks() {\n\t\tint cnt = 0;\n\t\tfor (int r = 0; r < H; r++) {\n\t\t\tfor (int c = 0; c < W; c++) {\n\t\t\t\tif (blocks[r][c] > 0)\n\t\t\t\t\tcnt++;\n\t\t\t}\n\t\t}\n\n\t\treturn cnt;\n\t}\n\n\t// blocks 복사\n\tstatic int[][] initBlocks(int[][] b) {\n\t\tint[][] copy = new int[H][W];\n\n\t\tfor (int r = 0; r < H; r++) {\n\t\t\tSystem.arraycopy(b[r], 0, copy[r], 0, W); // b[r]의 인덱스 0번부터 끝까지 copy[r]에 복사\n\t\t}\n\n\t\treturn copy;\n\t}\n\n\tstatic boolean isRange(int nr, int nc) {\n\t\t// 범위 벗어난 경우\n\t\treturn nr >= 0 && nr < H && nc >= 0 && nc < W;\n\t}\n}",
  "brick:minkyung": "import java.util.*;\n\nimport java.io.*;\n\npublic class Solution {\n\tstatic int[][] map;\n\tstatic int answer = 0;\n\tstatic int N, H, W;\n\t\n\tpublic static void main(String[] args) throws IOException {\n\t\tBufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n\t\tint T = Integer.parseInt(br.readLine());\n\t\tStringBuilder sb = new StringBuilder();\n\t\t\n\t\tfor (int t = 0; t < T; t++) {\n\t\t\tanswer = 0;\n\t\t\tStringTokenizer st = new StringTokenizer(br.readLine());\n\t\t\tN = Integer.parseInt(st.nextToken());\n\t\t\tW = Integer.parseInt(st.nextToken());\n\t\t\tH = Integer.parseInt(st.nextToken());\n\t\t\t\n\t\t\tmap = new int[H][W];\n\t\t\tint totalCount = 0;\n\t\t\t\n\t\t\tfor (int i = 0; i < H; i++) {\n\t\t\t\tst = new StringTokenizer(br.readLine());\n\t\t\t\tfor (int j = 0; j < W; j++) {\n\t\t\t\t\tmap[i][j] = Integer.parseInt(st.nextToken());\n\t\t\t\t\tif (map[i][j] != 0) {\n\t\t\t\t\t\ttotalCount++;\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t\t\n\t\t\tbacktrack(0, 0, map);\n\t\t\t\n\t\t\t\n\t\t\tsb.append(\"#\").append(t+1).append(\" \").append(totalCount - answer).append(\"\\n\");\n\t\t\t\n\t\t}\n\t\t\n\t\tSystem.out.println(sb.toString());\n\t}\n\t\n\tpublic static void backtrack(int count, int numOfBead, int[][] currentMap) {\n\t\t// 종료조건 \n\t\tif (count == N) {\n\t\t\tanswer = Math.max(numOfBead, answer);\n\t\t\treturn;\n\t\t}\n\t\t\n\t\tboolean found = false;\n\t\t\t\n\t\t// 맨 위에 있는 구슬만 가능 \n\t\tfor (int i = 0; i < H; i++) {\n\t\t\tfor (int j = 0; j < W; j++) {\n\t\t\t\t\n\t\t\t\t// 구슬 폭발 (맨 위에 있는 구슬만 가능)\n\t\t\t\tif (currentMap[i][j] != 0 && (i == 0 || currentMap[i-1][j] == 0)) {\n\t\t\t\t\tint numOfBumpCount = 0;\n\t\t\t\t\tfound = true;\n\t\t\t\t\t\n\t\t\t\t\tint[][] mapCopy = new int[H][W];\n\t\t\t\t\t\n\t\t\t\t\t// 원상복구를 위해 값 복사 \n\t\t\t\t\tfor (int k = 0; k < H; k++) {\n\t\t\t\t\t\tmapCopy[k] = currentMap[k].clone();\n\t\t\t\t\t}\n\t\t\t\t\t\n\t\t\t\t\tnumOfBumpCount = bump(i, j, mapCopy);\n\t\t\t\t\t// 중력 적용 \n\t\t\t\t\tdrop(mapCopy);\n\t\t\t\t\t\n\t\t\t\t\t// 다음 꺼 선택 \n\t\t\t\t\tbacktrack(count+1, numOfBead + numOfBumpCount, mapCopy);\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t\t\n\t\t// 터뜨릴 벽돌을 못찾은 경우 \n\t\tif (!found) {\n\t\t\tanswer = Math.max(numOfBead, answer);\n\t\t}\n\t}\n\t\n\t// 폭발 \n\tpublic static int bump(int y, int x, int[][] mapCopy) {\n\t\tint number = mapCopy[y][x] - 1;\n\t\tint numOfBeap = 1;\n\t\t\n\t\t// 폭발처리 \n\t\tmapCopy[y][x] = 0;\n\t\t\n\t\tfor (int i = y-number; i <= y+number; i++) {\n\t\t\tfor (int j = x-number; j <= x+number; j++) {\n\t\t\t\tif (i < 0 || i >= H || j < 0 || j >= W) continue;\n\t\t\t\t// 자기 자신 제외 \n\t\t\t\tif (i == y && j == x) continue;\n\t\t\t\t// 상하좌우만 탐색 \n\t\t\t\tif (!(i == y || j == x)) continue;\n\t\t\t\t\n\t\t\t\tif (mapCopy[i][j] != 0) {\n\t\t\t\t\tnumOfBeap += bump(i, j, mapCopy);\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t\t\n\t\treturn numOfBeap;\n\t}\n\t\n\t// 구슬 밑으로 떨어뜨리기 \n\tpublic static void drop(int[][] mapCopy) {\n\t\tfor (int j = 0; j < W; j++) {\n\t\t\tDeque<Integer> dq = new ArrayDeque<>();\n\t\t\t// 빈 공간 상관없이 숫자를 덱에 넣음\n\t\t\tfor (int i = 0; i < H; i++) {\n\t\t\t\tif (mapCopy[i][j] != 0) {\n\t\t\t\t\tdq.offerLast(mapCopy[i][j]);\n\t\t\t\t}\n\t\t\t}\n\t\t\t\n\t\t\tint index = H-1;\n\t\t\t\n\t\t\t// 다시 재정렬 \n\t\t\twhile (!dq.isEmpty()) {\n\t\t\t\tmapCopy[index][j] = dq.pollLast();\n\t\t\t\tindex--;\n\t\t\t}\n\t\t\t\n\t\t\t// 빈 공간 처리 \n\t\t\twhile (index >= 0) {\n\t\t\t\tmapCopy[index][j] = 0;\n\t\t\t\tindex--;\n\t\t\t}\n\t\t}\n\t}\n}",
  "block:jonghyuck": "package week5;\n\nimport java.util.*;\nimport java.io.*;\n\nclass Solution\n{\n    static List<Integer> list;\n//    static int [][] map;\n//    static int [][] temp;\n    static int maxValue;\n    static int N;\n    public static void main(String args[]) throws Exception\n    {\n\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n\n        int T = Integer.parseInt(br.readLine());\n        //System.setIn(new FileInputStream(\"res/input.txt\"));\n\n\t\t/*\n\t\t   표준입력 System.in 으로부터 스캐너를 만들어 데이터를 읽어옵니다.\n\t\t */\n\t\t/*\n\t\t   여러 개의 테스트 케이스가 주어지므로, 각각을 처리합니다.\n\t\t*/\n\n        // 2차원 정적 배열, stack으로 풀려고 해봤는데 결국\n        // remove했을 때 인덱스 땡겨지고 크기에 동적인 ArrayList가 최적\n        // dfs+백트래킹\n\n        for(int test_case = 1; test_case <= T; test_case++)\n        {\n            N = Integer.parseInt(br.readLine());\n\n            maxValue = Integer.MIN_VALUE;\n//            map = new int [N][N];\n//            temp = new int [N][N];\n            StringTokenizer st = new StringTokenizer(br.readLine());\n//            int [] arr = new int [N];\n//            for(int i = 0; i < N; i++) {\n//                arr[i] = Integer.parseInt(st.nextToken());\n//            }\n//\n//            for(int i = 0; i < N; i++) {\n//                for (int j = 0; j < N; j++) {\n//                    map[i][j] = arr[j];\n//                    temp[i][j] = arr[j];\n//                }\n//            }\n//\n//            maxValue = Integer.MIN_VALUE;\n\n\n            list = new ArrayList<>();\n\n            for(int i = 0; i < N; i++){\n                list.add(Integer.parseInt(st.nextToken()));\n            }\n\n            findMax(0);\n\n            StringBuilder sb = new StringBuilder();\n\n            sb.append('#').append(test_case).append(' ').append(maxValue);\n\n            System.out.println(sb);\n        }\n    }\n    static public void findMax(int score){\n        if(list.size() == 1){\n            maxValue = Math.max(maxValue, score + list.get(0));\n            return;\n        }\n        for(int i = 0; i < list.size(); i++){\n            int currentScore;\n\n            // 맨 왼쪽 블록 제거 할 대\n            if(i == 0){\n                currentScore = list.get(1);\n            }\n            // 맨 오른쪽 블록 제거 할 때\n            else if(i == list.size() - 1){\n                currentScore = list.get(i - 1);\n            }\n            // 가운데 블록 제거할 때\n            else{\n                currentScore = list.get(i - 1) * list.get(i + 1);\n            }\n\n\n            int removed = list.remove(i);\n\n            findMax(score + currentScore);\n\n            // list 복구\n            list.add(i, removed);\n        }\n\n\n    }\n\n}",
  "block:yeajeong": "package codingtest;\n\nimport java.util.*;\nimport java.io.BufferedReader;\nimport java.io.InputStreamReader;\n\npublic class Solution {\n//블록을 뽀갤때 모든경우의 수를 구해야한다.\n//\tcase\n//\t1. 좌우에 블록이 있는경우 -> 좌우 두 블록의 곱\n//\t2. 둘중 하나에만 이웃블록이 있는 경우 => 이웃한 블록에 적힌 숫자만큼 점수\n//\t3. 마지막남은 블록 -> 해당블록에 적힌 숫자만큼 점수\n//\t\n// 블록의 개수는 1이상 10이하 < 재귀가능\n// 리턴값 모든블록을 깨서 얻을 수 있는 최대 점수\n// 시뮬레이션. dfs, 모든 경우의 수 다 돌아\n\n\tstatic int[] bStatus;\n\tstatic int[] blocks;\n\tstatic int N;\n\tstatic int max;\n\n\tpublic static void main(String[] args) throws Exception {\n\t\t// 입력받기\n\t\tBufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n\t\tStringBuilder sb = new StringBuilder();\n\t\tStringTokenizer st;\n\n\t\tint test_case = Integer.parseInt(br.readLine());\n\n\t\tfor (int tc = 1; tc <= 10; tc++) {\n\n\t\t\tN = Integer.parseInt(br.readLine());\n\t\t\tblocks = new int[N];\n\t\t\tbStatus = new int[N];\n\n\t\t\tst = new StringTokenizer(br.readLine());\n\n\t\t\t// 질문하기 문자열 바로 배열못만듬?\n\t\t\tfor (int i = 0; i < N; i++) {\n\t\t\t\tblocks[i] = Integer.parseInt(st.nextToken());\n\t\t\t}\n\n\t\t\t// blocks 시뮬레이션\n\t\t\t// dfs로 돌림, 근데 현재상태를 체크하는 bstatus가 필요\n\t\t\tArrays.fill(bStatus, 1);\n\t\t\tmax = Integer.MIN_VALUE;\n\t\t\t// dfs 남은블록의 개수 4->3 3->2 2->1 1->0\n\t\t\tdfs(0 ,N );\n\t\t\t\n\t\t\tsb.append(\"#\").append(tc).append(\" \").append(max).append(\"\\n\");\n\t\t}\n\t\t\n\t\tSystem.out.println(sb);\n\n\t}\n\n\t// dfs구현\n\t// 현재까지의 점수 score와, 남은블록의 개수 restBlcok\n\tstatic void dfs(int score, int restBlock) {\n\n\t\t// base 조건 , max값 비교\n\t\tif (restBlock == 0) {\n\t\t\tmax = Math.max(max, score);\n\t\t\treturn ;\n\t\t}\n\n\t\t// 남은블록이, 몇개인지 case별로 비교 백트래킹\n\t\tfor (int i = 0; i < N; i++) {\n\n\t\t\t// 범위 아웃, 또는 이미방문했다면\n\t\t\tif (bStatus[i] == 0) continue;\n\t\t\t\n\t\t\tint left = hasBlock(0, i);\n\t\t\tint right = hasBlock(1, i);\n\n\n\t\t\tbStatus[i] = 0;\n\t\t\t\n\t\t\t//우측만 있을때\n\t\t\tif (left == -1 && right != -1) {\n\t\t\t\tdfs(score + blocks[right], restBlock - 1);\n\t\t\t} else if (right == -1 && left!= -1) { // 좌측만있을때\n\t\t\t\tdfs(score + blocks[left], restBlock - 1);\n\t\t\t}else if (left != -1 && left * right >= 0) { // 둘다있는경우\n\t\t\t\tint score2 = blocks[right] * blocks[left];\n\t\t\t\tdfs(score + score2, restBlock - 1);\n\t\t\t} // 한쪽만 \n\t\t\telse {// 둘다없어\n\t\t\t\tdfs(score + blocks[i], restBlock - 1);\n\t\t\t}\n\t\t\tbStatus[i] = 1;\n\n\t\t}\n\n\t}\n\n\t// 0은 왼쪽, 1은 오른쪽\n\tstatic int hasBlock(int dir, int index) {\n\n\t\n\t\tif (dir == 0) {\n\t\t\tint left = index - 1;\n\n\t\t\t// 0이아닌 숫자의 인덱스\n\t\t\t// 0이들어오면 blocks[-1] ->left = -1, blocks[-1] =  -1 \n\t\t\t// 1이들어오면 blocks[0] ->left = blocks[0], blocks[0]:: \n\t\t\t// \n\t\t\twhile (left >= 0 ) {\n\t\t\t\tif(bStatus[left] != 0) {\n\t\t\t\t\treturn left;\n\t\t\t\t}\n\t\t\t\tleft--;\n\t\t\t}\n\t\t\t\n\t\t\t//0이 아니면 벗어나고 left반환\n\t\t\treturn -1;\n\t\t} else {\n\t\t\tint right = index + 1;\n\n\t\t\t// 0이아닌 숫자의 인덱스\n\t\t\twhile (right < N) {\n\t\t\t\tif(bStatus[right] != 0) return right;\n\t\t\t\tright++;\n\t\t\t}\n\t\t\treturn -1;\n\t\t}\n\n\t}\n}",
  "block:taekgi": "// SWEA 블록 제거 게임\n/* \n * 블록 하나가 사라지면 빈 자리를 뒤에서 채워줘야함 -> 블록들을 동적 할당 배열인 ArrayList에 집어넣는다\n * \n * 전략 1.\n * 블록을 하나씩 깨서 최대 점수를 구해야 함\n * \t\t1부터 하나씩 출발해서 DFS (백트래킹)로 최대 점수를 구해야 할 거 같음\n * \t\t1-(2-3-4), (2-4-3), (3-2-4), (3-4-2), ...\n *\t\t2-(1-3-4), (1-4-3), (3-1-4), (3-4-1), ...\n *\t\t-> 이런 식으로 0번째 선택하고 나머지 수도 0,1,2번째 고르는 식으로 모든 경우의 수를 만들어 진행 \n *\t\t-> DFS 알고리즘을 사용해야 할 듯\n *\n * 그냥 배열에 블록들이 사라질 때까지 while문을 돌려서 작업을 수행\n * \t\t블록 하나 remove 함수 사용\n * \t\t왼쪽 이웃, 오른쪽 이웃 두개로 나누어서 점수를 획득\n * \n * block 사이즈에 따라 점수를 계산\n * \t\t1일 경우 남은 블록으로 점수 계산\n * \t\t양쪽 끝일 경우 이웃한 하나의 블록만 계산\n * \t\t양쪽에 이웃이 있을 경우 \n * \t\t\t왼쪽에 없으면 i+1로 가져와 계산\n * \t\t\t오른쪽에 없으면 i-1로 가져와 계산\n *\n * 해당 블록 제거 후 자기 자신의 함수 호출\n * 재귀 작업이 다 끝나면 복구를 위해 blocks에 추가\n * \n * \n * ----------------------------------------------------------------------\n *\n * 전략 2.\n * for문을 돌려서 왼쪽 이웃과 오른쪽 이웃이 있는지 확인한 후\n * \t\t있으면 큰 값의 점수를 가진 블록을 제거\n * \t\t없으면 왼쪽 혹은 오른쪽 이웃이 큰 점수를 가진 블록을 제거\n * 한 개밖에 없으면 나머지 점수 획득 처리\n * \n * \n * */\n\nimport java.util.*;\nimport java.io.*;\n\nclass Solution {\n\tstatic int N; // 블록의 개수 및 타격 기회\n\tstatic int ans; // 모든 블록을 깨서 얻을 수 있는 최대 점수\n\n\tstatic List<Integer> blocks;\n\n\tpublic static void main(String args[]) throws Exception {\n\t\tBufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n\t\tStringBuilder sb = new StringBuilder();\n\n\t\tint T = Integer.parseInt(br.readLine());\n\n\t\tfor (int test_case = 1; test_case <= T; test_case++) {\n\t\t\tN = Integer.parseInt(br.readLine());\n\t\t\tans = 0;\n\n\t\t\tStringTokenizer st = new StringTokenizer(br.readLine());\n\t\t\tblocks = new ArrayList<>();\n\t\t\tfor (int i = 0; i < N; i++) {\n\t\t\t\tblocks.add(Integer.parseInt(st.nextToken()));\n\t\t\t}\n\n//\t\t\tint initialIdx = initial(); // 첫 번째로 선택한 블록 인덱스\n\t\t\tremoveBlock(0);\n\n\t\t\tsb.append(\"#\").append(test_case).append(\" \").append(ans).append(\"\\n\");\n\t\t}\n\n\t\tSystem.out.print(sb);\n\t}\n\n//\t// 첫 번째 블록 선택\n//\tstatic int initial() {\n//\t\tint[] arr = new int[N];\n//\t\tint maxSum = 0;\n//\t\tint maxIndex = 0;\n//\t\t\t\n//\t\t// 한쪽만 있는 블록들(양 끝 블록들)보다 양쪽 다 있는 블록들이 더 클 거 같아서 1부터 N-2까지 탐색\n//\t\tfor(int i = 1; i < N-1; i++) {\n//\t\t\tint sum = blocks.get(i-1) * blocks.get(i+1);\n//\t\t\t\n//\t\t\tarr[i+1] = sum;\n//\t\t\t\n//\t\t\tif(maxSum < sum) {\n//\t\t\t\tmaxSum = sum;\n//\t\t\t\tmaxIndex = i+1;\n//\t\t\t}\n//\t\t}\n//\t\t\n//\t\treturn maxIndex;\n//\t}\n\n\t// 블록 부수기\n\tstatic void removeBlock(int result) {\n\t\t// 모든 블록을 다 부술 경우\n//\t\tif (blocks.isEmpty()) {\n//\t\t\tans = Math.max(ans, result);\n//\n//\t\t\treturn;\n//\t\t}\n//\n//\t\t// 왼쪽 이웃\n//\t\tif (index - 1 >= 0 && blocks.get(index - 1) > 0) {\n//\t\t\tleft = blocks.get(index - 1);\n//\t\t} else {\n//\t\t\tleft = 1; // 곱셈이기 때문에 1로 저장\n//\t\t}\n//\n//\t\t// 오른쪽 이웃\n//\t\tif (index + 1 < N && blocks.get(index - 1) > 0) {\n//\t\t\tright = blocks.get(index - 1);\n//\t\t} else {\n//\t\t\tright = 1;\n//\t\t}\n//\t\t\n//\t\t// 블록 하나 있으면 자기 자신의 값으로 점수 계산\n//\t\tif(blocks.size() == 1) {\n//\t\t\tresult += blocks.get(index);\n//\t\t}\n//\t\t\n//\t\tresult += left * right; // 점수 계산\n\n\t\tif (blocks.isEmpty()) {\n\t\t\tans = Math.max(ans, result);\n\n\t\t\treturn;\n\t\t}\n\n\t\tfor (int i = 0; i < blocks.size(); i++) {\n\n\t\t\tint score = 0; // 하나의 블록을 제거했을 때의 점수\n\n\t\t\tif (blocks.size() == 1) {\n\t\t\t\tscore = blocks.get(0);\n\t\t\t}\n\n\t\t\t// 왼쪽 블록이 없을 경우\n\t\t\telse if (i == 0) {\n\t\t\t\tscore = blocks.get(i + 1); // 유일하게 이웃하는 오른쪽 블록으로 점수 계산\n\t\t\t}\n\n\t\t\t// 오른쪽 블록이 없을 경우\n\t\t\telse if (i == blocks.size() - 1) {\n\t\t\t\tscore = blocks.get(i - 1); // 유일하게 이웃하는 왼쪽 블록으로 점수 계산\n\t\t\t}\n\n\t\t\t// 양쪽에 이웃이 있을 경우\n\t\t\telse {\n\t\t\t\tscore = blocks.get(i - 1) * blocks.get(i + 1);\n\t\t\t}\n\n\t\t\tint removed = blocks.remove(i); // 블록 제거 후 값 반환\n\n\t\t\tremoveBlock(result + score); // 다음 블록 탐색\n\n\t\t\tblocks.add(i, removed); // 재귀 작업이 끝나면 복구\n\t\t}\n\t}\n}",
  "block:minkyung": "import java.io.*;\n \npublic class Solution {\n    static List<Integer> blocks;\n    static int answer = 0;\n     \n    public static void main(String[] args) throws IOException {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        int T = Integer.parseInt(br.readLine());\n        StringBuilder sb = new StringBuilder();\n         \n        for (int t = 0; t < T; t++) {\n            int N = Integer.parseInt(br.readLine());\n            StringTokenizer st = new StringTokenizer(br.readLine());\n            blocks = new LinkedList<>();\n            answer = 0;\n             \n            for (int i = 0; i < N; i++) {\n                blocks.add(Integer.parseInt(st.nextToken()));\n            }\n             \n            backtrack(0);\n             \n            sb.append(\"#\").append(t+1).append(\" \").append(answer).append(\"\\n\");\n             \n        }\n         \n        System.out.println(sb.toString());\n    }\n     \n    public static void backtrack(int score) {\n        if (blocks.isEmpty()) {\n            answer = Math.max(score, answer);\n            return;\n        }\n         \n        for (int i = 0; i < blocks.size(); i++) {\n            int getScore = 0;\n             \n            if (blocks.size() == 1) {\n                getScore += blocks.get(i);\n            }\n            else if (i == 0) {\n                getScore += blocks.get(i+1);\n            }\n            else if (i == blocks.size() - 1) {\n                getScore += blocks.get(i-1);\n            }\n            else {\n                getScore += (blocks.get(i-1) * blocks.get(i+1));\n            }\n             \n            int removed = blocks.remove(i);\n             \n            backtrack(score + getScore);\n             \n            // 원상복구 \n            blocks.add(i, removed);\n        }\n    }\n}\n\n/*\n\nimport java.util.*;\n\nimport java.io.*;\n\npublic class homework1{\n    public static void main(String[] args) throws IOException {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        int T = Integer.parseInt(br.readLine());\n        StringBuilder sb = new StringBuilder();\n\n        for (int t = 0; t < T; t++) {\n            int N = Integer.parseInt(br.readLine());\n            StringTokenizer st = new StringTokenizer(br.readLine());\n            List<Integer> blocks = new LinkedList<>();\n\n            for (int i = 0; i < N; i++) {\n                blocks.add(Integer.parseInt(st.nextToken()));\n            }\n\n            Collections.sort(blocks);\n\n            int index = blocks.size() - 2;\n            int result = 0;\n\n            if (blocks.size() == 1) result += blocks.get(0);\n            else {\n                while (blocks.size() > 0 && index >= 0) {\n                    if (blocks.size() >= 3) {\n                        result += blocks.get(index+1) * blocks.get(index-1);\n                        index--;\n                    }\n                    else if (blocks.size() == 2) {\n                        result += blocks.get(index+1);\n                    }\n                    else {\n                        result += blocks.get(index);\n                    }\n\n                    blocks.remove(index);\n                }\n\n                System.out.println\n            }\n\n            sb.append(\"#\" + t).append(\" \").append(result).append(\"\\n\");\n        }\n\n        System.out.println(sb.toString());\n    }\n}\n\n*/"
};

