# 리팩토링 지침

## 목표

기능과 사용자 경험을 유지하면서 변경 범위를 예측 가능하게 만들고, 데이터 수집·표현·외부 API 호출의 책임을 분리한다.

## 기준

1. **단일 책임**: 페이지는 화면 조합과 선택 상태만 담당한다. 구문 강조, 마크다운 렌더링, 리뷰 요청과 캐시는 각각 별도 모듈에 둔다.
2. **의존성 방향**: `page/component → hook → lib` 방향을 유지한다. 범용 타입과 순수 함수는 UI를 import하지 않는다.
3. **타입 단일화**: API와 화면이 공유하는 응답 타입은 `app/lib`에서 한 번만 선언한다. 외부 응답은 런타임 검증 후 내부 타입으로 변환한다.
4. **서버 경계**: 토큰과 외부 API 호출은 Route Handler 안에 둔다. 클라이언트에는 공개 데이터만 전달한다.
5. **부작용 격리**: 네트워크 요청, `localStorage`, 전역 캐시는 hook 또는 서비스에 캡슐화한다. 렌더링 중에는 부작용을 만들지 않는다.
6. **요청 절약**: 동일 코드 요청은 해시 키로 중복 제거하고 캐시한다. AI 리뷰는 명시적인 사용자 동작으로만 요청한다.
7. **안전한 표시**: 사용자/Notion 텍스트는 HTML 문자열로 주입하지 않고 React 노드로 변환한다. 외부 링크는 `https?`만 허용한다.
8. **명확한 상태**: `idle/loading/ready/error` 상태를 명시하고 오류 메시지는 사용자용 문장으로 정규화한다.
9. **접근성**: 버튼의 비활성 상태, 링크 레이블, 키보드 포커스, 팝오버 닫기 동작을 유지한다.
10. **검증**: 변경 후 `npm run lint`와 `npm run build`를 모두 통과해야 한다. 기능 변경은 README에도 반영한다.

## 파일 구성

```text
app/
  components/       재사용 가능한 표시 컴포넌트
  hooks/            클라이언트 상태와 부작용
  lib/              공유 타입과 순수 도메인 로직
  api/              외부 서비스 연동과 입력 검증
  problems/[id]/    상세 페이지 조합
```

## 완료 조건

- 페이지 컴포넌트에 외부 API 세부 구현이나 파서 구현이 남지 않는다.
- 공유 타입이 API와 UI에서 중복 선언되지 않는다.
- 기존 캐시, 수동 리뷰, 코드 하이라이트, 전략/후기 마크다운 동작이 유지된다.
- 린트, 타입 검사, 프로덕션 빌드가 성공한다.
